{
  "schema_version": 1,
  "_meta": {
    "description": "Cross-lingual retrieval panel for POST-VERIFY-PLAN.md §10. Corpus is 50 docs across 5 technical topics (sorting, recursion, async I/O, SQL joins, ML basics), with each topic split into EN and VI variants — 5 docs per topic per language is 50 total, except topics where a doc serves dual duty (note: this corpus has 5 topics × 5 docs each = 25 docs in EN + 25 docs in VI = 50 docs). The gate: for each of 10 queries, top-3 retrieval by cosine MUST include ≥2 of the 3 hand-picked ground-truth doc IDs. Catches: (a) the model retrieves only same-language docs (multilingual training failed at retrieval time even if pairwise cosine looked fine), (b) the model retrieves topically-wrong docs (the embedding space is degenerate / over-smoothed).",
    "authored_by": "post-verify P1 spawn",
    "non_llm_authored": true,
    "topics": ["sorting", "recursion", "async_io", "sql_joins", "ml_basics"],
    "languages": ["en", "vi"],
    "retrieval_gate": "top3_must_include_at_least_2_of_ground_truth"
  },
  "corpus": [
    {"id": "sort-en-1", "lang": "en", "topic": "sorting", "text": "In Python, you can sort a list in place using `list.sort()` or get a new sorted copy with `sorted(list)`. Both accept a `key=` callable for custom orderings and a `reverse=True` flag for descending order. The underlying algorithm is Timsort, a hybrid stable sort that runs in O(n log n) worst case."},
    {"id": "sort-en-2", "lang": "en", "topic": "sorting", "text": "Quicksort is a divide-and-conquer comparison sort that picks a pivot and partitions the array into elements less than and greater than the pivot. Average time complexity is O(n log n) but worst case is O(n^2) when the pivot is always the smallest or largest element."},
    {"id": "sort-en-3", "lang": "en", "topic": "sorting", "text": "Merge sort is a stable, divide-and-conquer sort with guaranteed O(n log n) time complexity. It recursively splits the input into halves, sorts each half, then merges the two sorted halves into one. It requires O(n) auxiliary space, unlike quicksort."},
    {"id": "sort-en-4", "lang": "en", "topic": "sorting", "text": "To sort a list of dictionaries in Python by a specific key, pass a lambda to the `key` argument: `sorted(items, key=lambda x: x['age'])`. The same pattern works with `list.sort()`. For multiple keys, return a tuple from the lambda."},
    {"id": "sort-en-5", "lang": "en", "topic": "sorting", "text": "Heap sort uses a binary heap data structure to sort an array in O(n log n) time with O(1) auxiliary space. The algorithm first builds a max-heap from the input, then repeatedly extracts the maximum element and places it at the end of the array."},

    {"id": "sort-vi-1", "lang": "vi", "topic": "sorting", "text": "Trong Python, bạn có thể sắp xếp một danh sách tại chỗ bằng `list.sort()` hoặc lấy bản sao đã sắp xếp với `sorted(list)`. Cả hai đều nhận đối số `key=` để chỉ định hàm so sánh tùy chỉnh và cờ `reverse=True` để sắp xếp giảm dần. Thuật toán nội bộ là Timsort, độ phức tạp O(n log n) trong trường hợp xấu nhất."},
    {"id": "sort-vi-2", "lang": "vi", "topic": "sorting", "text": "Quicksort là thuật toán sắp xếp dựa trên so sánh theo nguyên lý chia để trị: chọn một phần tử trục (pivot) rồi chia mảng thành hai phần nhỏ hơn và lớn hơn trục. Độ phức tạp trung bình là O(n log n), nhưng trường hợp xấu nhất là O(n^2) khi trục luôn là phần tử nhỏ nhất hoặc lớn nhất."},
    {"id": "sort-vi-3", "lang": "vi", "topic": "sorting", "text": "Merge sort là thuật toán sắp xếp ổn định, theo nguyên lý chia để trị, với độ phức tạp đảm bảo O(n log n). Nó chia đệ quy đầu vào thành hai nửa, sắp xếp từng nửa, rồi trộn lại thành mảng sắp xếp. Tốn O(n) bộ nhớ phụ, khác với quicksort."},
    {"id": "sort-vi-4", "lang": "vi", "topic": "sorting", "text": "Để sắp xếp một danh sách các từ điển trong Python theo một khóa cụ thể, hãy truyền một lambda vào tham số `key`: `sorted(items, key=lambda x: x['age'])`. Cùng mẫu này hoạt động với `list.sort()`. Với nhiều khóa, trả về một tuple từ lambda."},
    {"id": "sort-vi-5", "lang": "vi", "topic": "sorting", "text": "Heap sort sử dụng cấu trúc dữ liệu heap nhị phân để sắp xếp mảng với độ phức tạp O(n log n) và O(1) bộ nhớ phụ. Thuật toán đầu tiên xây dựng max-heap từ đầu vào, sau đó liên tục lấy phần tử lớn nhất và đặt vào cuối mảng."},

    {"id": "rec-en-1", "lang": "en", "topic": "recursion", "text": "Recursion is a programming technique where a function calls itself to solve a smaller instance of the same problem. Every recursive function needs a base case to terminate and a recursive case that reduces the problem size."},
    {"id": "rec-en-2", "lang": "en", "topic": "recursion", "text": "The factorial of n can be computed recursively: `def factorial(n): return 1 if n <= 1 else n * factorial(n - 1)`. While elegant, deep recursion in Python can hit the recursion limit; for production code, an iterative loop is usually preferred."},
    {"id": "rec-en-3", "lang": "en", "topic": "recursion", "text": "Tail-call optimization (TCO) lets a compiler reuse the stack frame for a recursive call when the call is the last action in the function. Languages like Scheme and Scala guarantee TCO; Python does not, which is why deep Python recursion overflows the stack."},
    {"id": "rec-en-4", "lang": "en", "topic": "recursion", "text": "Memoization caches the results of expensive recursive calls so they don't get re-computed. Python's `@functools.lru_cache` decorator memoizes a function automatically. Fibonacci is the textbook example — naive recursion is O(2^n), memoized is O(n)."},
    {"id": "rec-en-5", "lang": "en", "topic": "recursion", "text": "Tree traversal is a natural fit for recursion: a function visits a node, then recursively visits each child. Pre-order, in-order, and post-order traversals differ only in where the 'visit current node' step happens relative to the recursive calls."},

    {"id": "rec-vi-1", "lang": "vi", "topic": "recursion", "text": "Đệ quy là kỹ thuật lập trình trong đó một hàm tự gọi chính nó để giải quyết một phiên bản nhỏ hơn của cùng bài toán. Mọi hàm đệ quy đều cần một trường hợp cơ sở để dừng và một trường hợp đệ quy thu nhỏ kích thước bài toán."},
    {"id": "rec-vi-2", "lang": "vi", "topic": "recursion", "text": "Giai thừa của n có thể được tính bằng đệ quy: `def factorial(n): return 1 if n <= 1 else n * factorial(n - 1)`. Tuy đẹp, đệ quy sâu trong Python có thể vượt giới hạn đệ quy; với mã sản phẩm, vòng lặp thường được ưu tiên hơn."},
    {"id": "rec-vi-3", "lang": "vi", "topic": "recursion", "text": "Tối ưu hóa lời gọi đuôi (TCO) cho phép trình biên dịch tái sử dụng khung ngăn xếp cho lời gọi đệ quy khi đó là hành động cuối cùng. Scheme và Scala đảm bảo TCO; Python thì không, đó là lý do đệ quy sâu trong Python gây tràn stack."},
    {"id": "rec-vi-4", "lang": "vi", "topic": "recursion", "text": "Memoization lưu cache kết quả của các lời gọi đệ quy tốn kém để tránh tính lại. Decorator `@functools.lru_cache` của Python tự động ghi nhớ. Fibonacci là ví dụ kinh điển — đệ quy ngây thơ là O(2^n), có memoize thì O(n)."},
    {"id": "rec-vi-5", "lang": "vi", "topic": "recursion", "text": "Duyệt cây là ứng dụng tự nhiên của đệ quy: hàm thăm một nút rồi đệ quy thăm từng con. Các thứ tự duyệt tiền tự, trung tự, hậu tự chỉ khác nhau ở vị trí của bước 'thăm nút hiện tại' so với các lời gọi đệ quy."},

    {"id": "async-en-1", "lang": "en", "topic": "async_io", "text": "Async I/O lets a program issue many concurrent I/O operations on a single thread by suspending each one while it waits for the OS to signal readiness. In Python, `asyncio` and the `async`/`await` keywords are the standard tools. The event loop drives all coroutines."},
    {"id": "async-en-2", "lang": "en", "topic": "async_io", "text": "In Rust, `tokio` is the dominant async runtime. You mark functions `async fn`, call them with `.await`, and spawn tasks with `tokio::spawn`. The runtime multiplexes many tasks over a small thread pool — exactly what high-throughput servers need."},
    {"id": "async-en-3", "lang": "en", "topic": "async_io", "text": "Async vs threads: async I/O is best when most of your time is spent waiting on the network or disk (high I/O, low CPU). Threads are better when you need to use multiple CPU cores in parallel for compute-bound work."},
    {"id": "async-en-4", "lang": "en", "topic": "async_io", "text": "A common pitfall in async code is blocking the event loop with a synchronous call. In Rust, wrap CPU-heavy work in `tokio::task::spawn_blocking`. In Python, use `loop.run_in_executor` or `asyncio.to_thread`."},
    {"id": "async-en-5", "lang": "en", "topic": "async_io", "text": "Backpressure is the ability for a consumer to slow down a producer when it can't keep up. Bounded channels (`tokio::sync::mpsc::channel(N)` in Rust) and bounded queues provide backpressure naturally — the producer blocks when the buffer is full."},

    {"id": "async-vi-1", "lang": "vi", "topic": "async_io", "text": "Async I/O cho phép một chương trình thực thi nhiều thao tác I/O đồng thời trên một luồng duy nhất bằng cách tạm dừng từng thao tác trong khi chờ OS báo sẵn sàng. Trong Python, `asyncio` và các từ khóa `async`/`await` là công cụ chuẩn. Vòng lặp sự kiện điều khiển tất cả các coroutine."},
    {"id": "async-vi-2", "lang": "vi", "topic": "async_io", "text": "Trong Rust, `tokio` là runtime async chủ đạo. Bạn đánh dấu hàm `async fn`, gọi chúng với `.await`, và sinh task bằng `tokio::spawn`. Runtime ghép nhiều task lên một pool luồng nhỏ — đúng thứ máy chủ thông lượng cao cần."},
    {"id": "async-vi-3", "lang": "vi", "topic": "async_io", "text": "Async so với thread: async I/O tốt nhất khi phần lớn thời gian bị chặn chờ mạng hoặc đĩa (I/O cao, CPU thấp). Thread tốt hơn khi bạn cần dùng nhiều lõi CPU song song cho công việc nặng tính toán."},
    {"id": "async-vi-4", "lang": "vi", "topic": "async_io", "text": "Một lỗi phổ biến trong mã async là chặn vòng lặp sự kiện bằng một lời gọi đồng bộ. Trong Rust, gói công việc nặng CPU vào `tokio::task::spawn_blocking`. Trong Python, dùng `loop.run_in_executor` hoặc `asyncio.to_thread`."},
    {"id": "async-vi-5", "lang": "vi", "topic": "async_io", "text": "Backpressure là khả năng người tiêu dùng làm chậm nhà sản xuất khi không theo kịp. Kênh có giới hạn (`tokio::sync::mpsc::channel(N)` trong Rust) và hàng đợi có giới hạn cung cấp backpressure tự nhiên — nhà sản xuất bị chặn khi bộ đệm đầy."},

    {"id": "sql-en-1", "lang": "en", "topic": "sql_joins", "text": "An INNER JOIN returns only rows where the join condition matches in both tables. If a row in the left table has no match on the right, it's excluded. This is the default JOIN type and the one to use when you want strictly paired rows."},
    {"id": "sql-en-2", "lang": "en", "topic": "sql_joins", "text": "A LEFT OUTER JOIN returns every row from the left table, plus matching rows from the right. Where there's no match on the right, the right-side columns come back as NULL. Use it to keep all rows from the primary table regardless of the join."},
    {"id": "sql-en-3", "lang": "en", "topic": "sql_joins", "text": "A FULL OUTER JOIN returns every row from both tables, with NULL filling in the unmatched side. It's the union of LEFT JOIN and RIGHT JOIN. Less common in OLTP, useful for reconciliation queries."},
    {"id": "sql-en-4", "lang": "en", "topic": "sql_joins", "text": "Use EXPLAIN ANALYZE in PostgreSQL to see the actual execution plan for a JOIN. The planner picks between nested loop, hash join, and merge join based on the size of each side and available indexes. Missing indexes on join keys is the #1 cause of slow queries."},
    {"id": "sql-en-5", "lang": "en", "topic": "sql_joins", "text": "A CROSS JOIN returns the Cartesian product of two tables — every row on the left paired with every row on the right. Rarely useful directly; mostly appears implicitly when a JOIN condition is forgotten."},

    {"id": "sql-vi-1", "lang": "vi", "topic": "sql_joins", "text": "INNER JOIN chỉ trả về các hàng mà điều kiện join khớp ở cả hai bảng. Nếu một hàng ở bảng bên trái không có khớp bên phải, nó bị loại. Đây là kiểu JOIN mặc định và là lựa chọn khi bạn muốn các cặp hàng được ghép chặt chẽ."},
    {"id": "sql-vi-2", "lang": "vi", "topic": "sql_joins", "text": "LEFT OUTER JOIN trả về mọi hàng từ bảng bên trái, cộng với các hàng khớp ở bên phải. Khi không có khớp ở bên phải, các cột bên phải trả về NULL. Dùng để giữ tất cả hàng từ bảng chính bất kể join."},
    {"id": "sql-vi-3", "lang": "vi", "topic": "sql_joins", "text": "FULL OUTER JOIN trả về mọi hàng từ cả hai bảng, với NULL điền vào bên không khớp. Đây là hợp của LEFT JOIN và RIGHT JOIN. Ít gặp trong OLTP, hữu ích cho các truy vấn đối chiếu."},
    {"id": "sql-vi-4", "lang": "vi", "topic": "sql_joins", "text": "Dùng EXPLAIN ANALYZE trong PostgreSQL để xem kế hoạch thực thi thực tế cho một JOIN. Planner chọn giữa nested loop, hash join và merge join dựa trên kích thước hai bên và các chỉ mục có sẵn. Thiếu chỉ mục trên khóa join là nguyên nhân số 1 của truy vấn chậm."},
    {"id": "sql-vi-5", "lang": "vi", "topic": "sql_joins", "text": "CROSS JOIN trả về tích Đề-các của hai bảng — mỗi hàng bên trái ghép với mỗi hàng bên phải. Hiếm khi dùng trực tiếp; thường xuất hiện ngầm khi điều kiện JOIN bị bỏ sót."},

    {"id": "ml-en-1", "lang": "en", "topic": "ml_basics", "text": "Supervised learning trains a model on labeled examples — pairs of input and expected output. The model learns a function from inputs to outputs, then predicts labels for new, unseen inputs. Classification and regression are the two main flavors."},
    {"id": "ml-en-2", "lang": "en", "topic": "ml_basics", "text": "Overfitting happens when a model memorizes the training data rather than learning the underlying pattern. The signal is high training accuracy but low test accuracy. Combat it with more data, regularization, dropout, or simpler model architectures."},
    {"id": "ml-en-3", "lang": "en", "topic": "ml_basics", "text": "Gradient descent is the workhorse optimization algorithm: compute the gradient of the loss with respect to each parameter, then nudge each parameter a small step in the opposite direction. Variants like Adam adapt the step size per parameter."},
    {"id": "ml-en-4", "lang": "en", "topic": "ml_basics", "text": "A neural network is a stack of layers, each applying a linear transformation followed by a nonlinear activation (ReLU, GELU). Training adjusts the weights of each linear layer via backpropagation — the chain rule applied across the whole stack."},
    {"id": "ml-en-5", "lang": "en", "topic": "ml_basics", "text": "Embeddings are dense vector representations of discrete objects (words, sentences, images). Similar inputs map to nearby vectors. They power semantic search, recommendation systems, and retrieval-augmented generation."},

    {"id": "ml-vi-1", "lang": "vi", "topic": "ml_basics", "text": "Học có giám sát huấn luyện mô hình trên các ví dụ có nhãn — cặp đầu vào và đầu ra mong muốn. Mô hình học một hàm từ đầu vào sang đầu ra, rồi dự đoán nhãn cho đầu vào mới chưa thấy. Phân loại và hồi quy là hai kiểu chính."},
    {"id": "ml-vi-2", "lang": "vi", "topic": "ml_basics", "text": "Overfitting xảy ra khi mô hình ghi nhớ dữ liệu huấn luyện thay vì học mẫu nền tảng. Dấu hiệu là độ chính xác cao trên tập huấn luyện nhưng thấp trên tập kiểm tra. Chống lại bằng nhiều dữ liệu hơn, regularization, dropout, hoặc kiến trúc đơn giản hơn."},
    {"id": "ml-vi-3", "lang": "vi", "topic": "ml_basics", "text": "Gradient descent là thuật toán tối ưu chủ đạo: tính gradient của hàm mất mát theo từng tham số, rồi đẩy mỗi tham số một bước nhỏ ngược chiều. Các biến thể như Adam tự điều chỉnh kích thước bước cho từng tham số."},
    {"id": "ml-vi-4", "lang": "vi", "topic": "ml_basics", "text": "Mạng nơ-ron là một chồng các tầng, mỗi tầng áp dụng một biến đổi tuyến tính rồi đến một hàm kích hoạt phi tuyến (ReLU, GELU). Huấn luyện điều chỉnh trọng số của từng tầng tuyến tính qua lan truyền ngược — quy tắc chuỗi áp dụng xuyên suốt chồng."},
    {"id": "ml-vi-5", "lang": "vi", "topic": "ml_basics", "text": "Embedding là biểu diễn vector dày đặc của các đối tượng rời rạc (từ, câu, ảnh). Đầu vào giống nhau ánh xạ tới các vector gần nhau. Chúng hỗ trợ tìm kiếm ngữ nghĩa, hệ gợi ý và sinh có truy xuất tăng cường."}
  ],
  "queries": [
    {
      "id": "q-en-sort-python",
      "lang": "en",
      "text": "how to sort a list in Python",
      "ground_truth_top3": ["sort-en-1", "sort-en-4", "sort-vi-1"]
    },
    {
      "id": "q-en-recursion",
      "lang": "en",
      "text": "what is recursion and when does it overflow the stack",
      "ground_truth_top3": ["rec-en-1", "rec-en-3", "rec-vi-1"]
    },
    {
      "id": "q-en-async-tokio",
      "lang": "en",
      "text": "how does the tokio runtime schedule async tasks in Rust",
      "ground_truth_top3": ["async-en-2", "async-en-4", "async-vi-2"]
    },
    {
      "id": "q-en-left-join",
      "lang": "en",
      "text": "difference between INNER JOIN and LEFT OUTER JOIN in SQL",
      "ground_truth_top3": ["sql-en-1", "sql-en-2", "sql-vi-2"]
    },
    {
      "id": "q-en-overfitting",
      "lang": "en",
      "text": "how to prevent overfitting in a machine learning model",
      "ground_truth_top3": ["ml-en-2", "ml-vi-2", "ml-en-1"]
    },

    {
      "id": "q-vi-sort-python",
      "lang": "vi",
      "text": "làm sao để sắp xếp một danh sách trong Python",
      "ground_truth_top3": ["sort-vi-1", "sort-vi-4", "sort-en-1"]
    },
    {
      "id": "q-vi-recursion",
      "lang": "vi",
      "text": "đệ quy là gì và khi nào nó gây tràn stack",
      "ground_truth_top3": ["rec-vi-1", "rec-vi-3", "rec-en-1"]
    },
    {
      "id": "q-vi-async-tokio",
      "lang": "vi",
      "text": "tokio lập lịch các task async trong Rust như thế nào",
      "ground_truth_top3": ["async-vi-2", "async-vi-4", "async-en-2"]
    },
    {
      "id": "q-vi-left-join",
      "lang": "vi",
      "text": "khác biệt giữa INNER JOIN và LEFT OUTER JOIN trong SQL",
      "ground_truth_top3": ["sql-vi-1", "sql-vi-2", "sql-en-2"]
    },
    {
      "id": "q-vi-overfitting",
      "lang": "vi",
      "text": "làm sao để tránh overfitting khi huấn luyện mô hình học máy",
      "ground_truth_top3": ["ml-vi-2", "ml-en-2", "ml-vi-1"]
    }
  ]
}
