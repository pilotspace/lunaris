//! `lunaris-hook` library — hook envelope processing.
//!
//! RED STUB: modules declared but not implemented. Integration tests will
//! fail because the binary exits 73 unconditionally in this state.

#![forbid(unsafe_code)]
#![deny(rust_2018_idioms, unreachable_pub)]

pub mod envelope;
pub mod ingest;
pub mod scope;
