Create a calm, light, hand-drawn style architecture diagram on a pure white (#FAFAFA) background, like a clean engineering notebook sketch: thin cool-charcoal ink lines (#404045), generous whitespace, softly rounded corners. Accent color: muted teal (#5a9a8a), used sparingly.

IMPORTANT TYPOGRAPHY RULE: All text uses CLEAN, HIGHLY LEGIBLE PRINT LETTERING — neat block lettering / clean sans-serif look. NOT cursive, NOT messy handwriting. Every word spelled exactly as written below.

SERIES NUMERAL: a hand-drawn circle at the top-left containing the printed numeral 04 in bold charcoal — this is a REQUIRED VISIBLE TEXT ELEMENT.

GEOMETRIC PRIMITIVES ONLY: use rectangular boxes for structs/fields, stacked horizontal planes for collections, cylinders for databases, node-circle-and-edge for graphs. No freehand blobs. All corners softly rounded.

TITLE at top center: "GRAPH PRIMITIVES" in charcoal print lettering.
Subtitle in lighter gray (#8a8a85): "step 4 of 8 — entity / relation / fact  (opt-in)"

LAYOUT: three columns, each a different geometric shape.

LEFT COLUMN — Entity: a rounded rectangle (soft cloud-blue tint #eef3f9).
Label at top: "Entity"
Inside: stacked field boxes: "id : ULID"  "scope"  "name : str"  "aliases : Vec<str>"  "entity_type"  "embedding : Vec<f32>"  "bt : BiTemporal"  "confidence : f32"
Pencil annotation: "blake3 deterministic EntityId"

CENTER COLUMN — Relation: show two entity circles connected by a labeled arrow.
Circle (left): "Entity A"  Arrow labeled "rel_type" pointing right  Circle (right): "Entity B"
Label above: "Relation"
Below the arrow, small stacked field text: "id  scope  src  dst  rel_type  bt  confidence  provenance"
Pencil annotation: "directed typed edge"

RIGHT COLUMN — Fact: a rounded rectangle (soft sage tint #eef5ee).
Label at top: "Fact"
Inside: stacked field boxes: "subject"  "predicate"  "object"  "fact_text : str"  "embedding : Vec<f32>"  "activation : f32"  "bt : BiTemporal"  "confidence : f32"  "provenance"
Pencil annotation on activation: "ACT-R recency weight"

A thin dotted border around all three columns with label "opt-in extraction  OR  structured ingest"

CONNECTIONS: main flow arrows thin solid lines with small open arrowheads.
Annotation leader lines are thin, light pencil-gray strokes ending in small dots.

AVOID: no solid fills, no heavy color blocks, no bold strokes, no drop shadows, no gradients, no dark background, no emoji, no clipart, no computer-perfect ruler lines, no cursive handwriting. Colors only as 15% opacity watercolor tints. Generous margins, everything breathes.
CRITICAL spelling (zero tolerance — regenerate if wrong): ULID must be U-L-I-D, BiTemporal must be B-i-T-e-m-p-o-r-a-l, blake3 must be b-l-a-k-e-3 (lowercase b, digit 3 not letter), BM25 must be B-M-2-5.
