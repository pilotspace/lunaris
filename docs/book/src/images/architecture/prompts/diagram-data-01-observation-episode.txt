Create a calm, light, hand-drawn style architecture diagram on a pure white (#FAFAFA) background, like a clean engineering notebook sketch: thin cool-charcoal ink lines (#404045), generous whitespace, softly rounded corners. Accent color: muted teal (#5a9a8a), used sparingly.

IMPORTANT TYPOGRAPHY RULE: All text uses CLEAN, HIGHLY LEGIBLE PRINT LETTERING — neat block lettering / clean sans-serif look. NOT cursive, NOT messy handwriting. Every word spelled exactly as written below.

SERIES NUMERAL: a hand-drawn circle at the top-left containing the printed numeral 01 in bold charcoal — this is a REQUIRED VISIBLE TEXT ELEMENT.

GEOMETRIC PRIMITIVES ONLY: use rectangular boxes for structs/fields, stacked horizontal planes for collections, cylinders for databases, node-circle-and-edge for graphs. No freehand blobs. All corners softly rounded.

TITLE at top center: "OBSERVATION → EPISODE" in charcoal print lettering.
Subtitle in lighter gray (#8a8a85): "step 1 of 8 — the input envelope"

LAYOUT: one large rounded rectangle labeled "Episode" (soft cloud-blue tint #eef3f9).
Inside the rectangle, show two zones separated by a thin dashed line:

Zone A (left half, labeled "identity"): three stacked field boxes in a small column:
  box "id : ULID"  box "scope : Scope"  box "source : str"
  Pencil annotation on "id": "ULID — sortable unique id"

Zone B (right half, labeled "content + provenance"): four stacked field boxes:
  box "content : str"  box "t_ref : Timestamp"  box "metadata : Map"  box "bt : BiTemporal"
  Pencil annotation on "bt": "valid + system intervals — HLC-stamped"

Above the Episode box, a small rounded input box labeled "Observation" with a thin arrow flowing down into Episode.
Pencil annotation on the arrow: "raw agent turn, tool result, or document"

CONNECTIONS: main flow arrows thin solid lines with small open arrowheads.
Annotation leader lines are thin, light pencil-gray strokes ending in small dots.

AVOID: no solid fills, no heavy color blocks, no bold strokes, no drop shadows, no gradients, no dark background, no emoji, no clipart, no computer-perfect ruler lines, no cursive handwriting. Colors only as 15% opacity watercolor tints. Generous margins, everything breathes.
CRITICAL spelling (zero tolerance — regenerate if wrong): ULID must be U-L-I-D, BiTemporal must be B-i-T-e-m-p-o-r-a-l (capital B, lowercase i, capital T).
