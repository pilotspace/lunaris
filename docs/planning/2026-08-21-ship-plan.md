# Lunaris production ship plan — task ledger

**Created** 2026-08-21 · **Audited ref** `main` @ `aba6a44` (v0.7.0)
**Report artifact** https://claude.ai/code/artifact/81a7bcc7-4dac-46e7-942d-fe66b81ce4f1

Derived from four parallel read-only audits (repository status, benchmark
evidence, first-five-minutes DX, TencentDB-Agent-Memory teardown). This file is
the shared context every delegated agent reads. Check a box only when the work
is merged, not when it is written.

---

## The thesis

Lunaris does not need more engine. Four audits agree the retrieval core is
strong and the defects sit **at every boundary where the project meets a
stranger**. The competitive read reinforces it: TencentDB-Agent-Memory has
23.4k stars behind 173 files, 9 test files, one CI workflow and **zero
benchmark harness** — they have the story, we have the substance. The work is
to open the front door, stop the public surfaces reporting success falsely, and
make our numbers reproducible when theirs are not.

## The blocking fact

Lunaris 0.7.0 hard-rejects any Moon below `0.8.5` at connect
(`crates/lunaris-storage-moon/src/version.rs`). **Moon v0.8.5 is a published
release with zero binary assets** — verified `v0.8.5 assets=0` against
`v0.8.4 assets=48` / `v0.8.3 assets=48`; all platform tarballs 404; ghcr
returns `401` to an anonymous pull against a `200` control. The v0.8.4 binaries
that exist are refused by the handshake.

The only working Moon 0.8.5 is the source build at `~/.lunaris/bin/moon` on the
owner's laptop. This is an **undocumented internal-only dependency**, not a
broken release. Until W0.1 ships, every other task improves a product nobody
can start.

---

## W0 — Unblock

*Exit gate: a stranger on a clean machine reaches a first recall in under five
minutes, from published artifacts only, with no source builds.*

- [ ] **W0.1** Re-cut Moon v0.8.5 release assets; make the ghcr package public — **S** — `OWNER-BLOCKED` (needs admin on `pilotspace/moon`)
- [x] **W0.2** CI smoke job running `install.sh` against the newest tag on macOS + Linux — **S** — DONE 2026-08-21 (`.github/workflows/moon-install-smoke.yml`).
      Not the Lunaris installer — the guides point readers at **Moon's** curl one-liner, and Lunaris refuses at
      connect any Moon below `MIN_MOON_VERSION`. So this job tests the exact thing W0.1 is blocked on, and it is
      expected RED until the assets are re-cut: re-verified 2026-08-21 that moon v0.8.5 still has 0 assets vs 48
      on v0.8.4. Its value is turning that from a sentence in this document into a signal that flips green by
      itself the moment the assets land, and red again if they vanish.
      **Scheduled + `workflow_dispatch` only, never on push/PR** — it depends on a third-party release no
      contributor's PR can fix, so blocking merges on it would punish people for a missing tarball that is not
      theirs. It reads `MIN_MOON_VERSION` out of `version.rs` rather than hardcoding it (a hardcoded copy would
      drift and then approve a Moon that Lunaris rejects), and both parse-failure modes were mutation-checked.
      Starts no server, so it touches no port.
- [x] **W0.3** Add `-e LUNARIS_MCP_STORAGE=…` to both `claude mcp add` lines and the JSON block — **S** — DONE 2026-08-21 (`3ba7307`).
      Eleven bare invocations, not two: README (2 + the JSON block), `docs/integration/claude-code.md` (4),
      the book (5), and — found last, and the two that matter most — `crates/lunaris-mcp/README.md` and
      `crates/lunaris-mcp-npm/README.md`, the pages crates.io and npmjs.com render. Gate:
      `scripts/tests/test_mcp_add_examples_boot.py`. Its own first draft self-matched its grep literal in
      `ci.yml` and had a 4-line lookahead that read the *next* example's flag, so it passed while broken —
      caught only by its own mutation check, which is why the rewrite reads each command to its exact end.
- [x] **W0.4** Zero-config trial path — `lunaris try` over the existing `embedded-moon` feature (`f61ecae` RED, `c941082` GREEN, branch `feat/lunaris-try`) — **M** — DONE 2026-08-21.
      Ships `crates/lunaris-cli/src/{corpus,stage,trial}.rs` + `docs/quickstart-try.md`, spliced into the README
      above *Pick your path*. Notably it does **not** depend on W0.1: the trial launches an in-process Moon, so
      it is the one front door that works while the 0.8.5 release assets are still missing. Guards: the trial
      ignores every `LUNARIS_*` store variable and refuses 6379/6380/6381/6399, and
      `embedded_moon_is_never_in_a_default_feature_set()` now scans **every** crate manifest, not just the CLI's.
      Scope note: `npx lunaris try` was NOT delivered and is folded into W0.9 — see below.
- [x] **W0.9** A release job that builds the `lunaris` CLI — **M** — DONE 2026-08-21 (`de998ff`).
      `.github/workflows/cli-release.yml` mirrors `mcp-prebuild.yml` (same five triples, `fail-fast: false`, the
      aarch64-linux cross toolchain, and the `!cancelled()` guards from the v0.6.0-rc.1 lesson where one failing
      leg silently discarded the whole distribution). Builds with `--features embedded-moon` on the command line;
      no manifest touched. Artifacts are `lunaris-cli-*`, never `lunaris-*` — the latter glob would sweep up
      `lunaris-mcp-*` on the same tag. Adds a `cargo tree` gate BEFORE the expensive build asserting the feature
      actually reaches the vendor/moon server crate (1 match with, 0 without): a silent feature-forwarding break
      here would ship a green release whose `lunaris try` refuses.
      **Validated on aarch64-apple-darwin only**: 6m02s build, `--version` → `lunaris 0.7.0`, `--help` lists `try`,
      22 MB binary WITHOUT the `NO_EMBEDDED_MOON` refusal string vs 13 MB with it when the feature is off.
      **Unvalidated and stated as such**: four of five legs (Windows highest risk — neither CLI nor Moon server
      has ever been built for `x86_64-pc-windows-msvc` here), runner wall-clock, and the entire tag-gated upload
      job. Proving the upload needs a throwaway pre-release tag; `workflow_dispatch` needs the file on `main`
      first. **Owner secrets required: none** — only the automatic `GITHUB_TOKEN`.
      Carried correction from the agent: it first "proved" the server was linked by grepping the binary for
      `BGREWRITEAOF`/`FT.NAVIGATE`; the negative control showed those come from the moondb **client** and appear
      in both builds. That check proved nothing, and the workflow header says so to stop anyone re-adding it.
      Still not delivered: `npx lunaris try` (no npm wrapper crate exists — W2.14's call).
      `docs/quickstart-try.md` claimed "the published release binaries are built with it". No workflow in
      `.github/workflows/` builds `lunaris-cli` at all — only `lunaris-mcp` is prebuilt, and it ships inside the
      `npx`/`uvx` wrapper packages rather than as a loose binary. So the front door currently opens only after a
      `git clone` + `cargo build --release -p lunaris-cli --features embedded-moon`. The doc and the README now
      say exactly that; this task is what makes the shorter promise true. Mirror `mcp-prebuild.yml` (same five
      targets), build **with** `--features embedded-moon`, and note it cannot be validated without pushing a tag —
      so it needs a dry-run on a throwaway pre-release tag before it counts as verified, or it is just another
      writer with no reader.
- [x] **W0.5** `.query(text)` on both DSL builders + README samples (`12a74af`, `b247c29`, branch `feat/sdk-query-dsl`) — **M** — DONE 2026-08-21
- [x] **W0.6** One ergonomic layer for ESM + CJS; entry moved to `lunaris.cjs` / `lunaris.d.ts` (`b247c29`) — **M** — DONE 2026-08-21
- [ ] **W0.7** Promote `model_stager` into `lunaris-core`, or hard-error on the Noop fallback behind an explicit opt-out — **M**

## W1 — Integrity

*Exit gate: no shipped surface reports success for work it did not do, and no
CI gate can pass without asserting something.*

- [x] **W1.1** `/v1/forget` routes through `claims.scope`; zero-match single-ULID returns 404 (not 403 — a 403 makes the endpoint a cross-tenant existence oracle); `ignored_test_ratchet` added (`3914fe6`, `28465ca`) — **S** — **P0** — DONE 2026-08-21
- [x] **W1.2** `default = ["llamacpp"]` on lunaris-server; `/readyz` fails on a Noop backend via a structural backend-identity signal; manifest guard extended (`f311439`) — **S** — **P0** — DONE 2026-08-21
- [ ] **W1.3** Branch protection on `main`; ratchet + integration + conformance required on PRs; delete the `|| exit 0` escapes — **S** — `OWNER-BLOCKED` (needs admin)
- [x] **W1.4** Extend `forget` to chunk rows — **M** — CHUNK HALF DONE 2026-08-22; fact half split out (see below)
      **Traced 2026-08-22 — the defect is sharper than this line, and the fact half is a different task.**
      1. **The `bt` half is already done.** `hydrate.rs:57` gates on `bt.sys.1.is_some()`, and the
         soft-forget path stamps it. Nothing to write.
      2. **A HARD forget leaves the content recallable — this is the real bug.** `scan_matches_scoped`
         walks one prefix, `lunaris:{scope}:episode:`, so chunk rows are never matched. Recall stays
         clean under a SOFT forget only because `hydrate`'s third pass drops chunks whose parent episode
         is sys-closed:
         `.filter(|(_, chunk)| !matches!(episode_sources.get(&chunk.episode_id), Some((_, true))))`
         A hard forget `KvDelete`s the episode row, so that lookup returns **`None`**, and
         `!matches!(None, Some((_, true)))` is `true` — the chunk is kept and hydrates with an empty
         `source`. **The irreversible, D-21 confirmation-token-gated path is the leaky one**; the
         reversible default works. Two locally reasonable decisions compose into it, and each documents
         itself honestly ("forget v0 stamps only the `episode:` row" / "Missing episodes stay tolerated").
         Fix: `scan_matches_scoped` must also collect the matched episodes' chunk rows. Costs one prefix
         scan over the scope's chunks per call — including on the `ForgetTarget::Id` fast path, since no
         reverse episode→chunk index exists (ingest writes doctree/episode/chunk/vector and no back-link).
      3. **The fact half is NOT implementable as written** — it needs a schema change, not a wider scan:
         - `lunaris_extract::Fact` carries **no episode or chunk provenance**. `RawExtraction` has
           `source_chunk_id`, and it is dropped when the validated `Fact` is persisted
           (`crates/lunaris/src/ingest.rs:709`).
         - The fact key is `FactId::from_triple(subject, predicate, object)` — **content-derived**, so two
           episodes asserting the same fact write the SAME row. Deleting it on one episode's forget would
           erase another episode's assertion.
         Same shape for entities and relations (`EntityId = blake3(name || type)`) and communities.
         Doing this properly means per-fact contributing-episode provenance plus reference-counted
         deletion. Split it out; it is not an M.
      4. **`classify_indices` overclaims.** Every target reports `Kv + Vector + Graph` affected, but a
         forget only ever writes KV rows. The vector embedding and BM25 text stay in Moon's FT index
         after a hard delete — recall is clean (hydrate drops a hit whose chunk row is gone), but the
         bytes are still there, which is not what "hard delete" promises.
      5. Not carried in the chunk fix: the `doctree:` row (keyed BY episode id, so trivially addressable)
         has no `bt` field, and `build_soft_delete_op` silently writes the payload back unpatched when
         `bt.sys` is absent — a stamp that does not stamp. Including it needs that helper to fail loudly
         first.
      **DONE (chunk half).** `scan_matches_scoped` now sweeps the matched episodes' chunk rows, so a soft
      forget stamps them and a hard forget deletes them — neither depends on the episode row surviving.
      Full RED/GREEN against a live Moon, with the fix stashed for the red so it could not compile itself in:
      RED — `hard_forget_leaves_no_recallable_text_moon` FAILED ("the verbatim text survived a confirmed
      hard delete"), `hard_forget_removes_the_content_from_recall_moon` FAILED (`rows_deleted=1` with the
      hit still present and `source: ""`, the blank-provenance fingerprint of the missing episode row),
      5 pre-existing forget tests green throughout. GREEN — 7 passed / 0 failed.
      **Contract change, in CHANGELOG:** `rows_written`/`rows_deleted` now count ROWS, not episodes, so
      `forget(Id)` on a one-chunk episode reports 2. Four assertions moved to the discriminating `> 1`
      form (the one that fails if the chunk sweep is removed) rather than a brittle exact count; the
      conformance `rows_deleted == 1` became `>= 1`, keeping the zero-guard the P0 needs.
      **`matched` and the MCP `removed` stayed EPISODES — and getting that wrong is what CI caught.**
      The first draft let the row count leak through both, because `matched` was `matches.len()` and
      `removed` was `rows_written + rows_deleted`: two numbers that happened to equal the episode
      count right up until chunks were swept. Three `lunaris-memory-service` unit tests and the
      protocol suite's `id_target` went red on the PR. They were right to. An agent asked to confirm
      a deletion reasons in episodes — "about to delete 2 things" is a worse answer than "1 episode",
      and both DTOs document episodes. `matched` is now counted by episode-key prefix (NOT by parsing
      payloads for an `id`, which a `Chunk` also carries), and `removed` is `matched` on a committing
      call, 0 on a preview, read from the engine's own `preview` flag so the two cannot disagree.
      Two surfaces, two units, each named for what it counts.
      **Split out, NOT done:** (a) facts/entities/relations/communities — blocked on the content-hash
      keying + missing provenance described above; (b) the FT-index residue and the `classify_indices`
      overclaim; (c) the `doctree:` row.

- [x] **W1.5** `llamacpp-wired` job stages + caches the GGUFs AND greps the log for `[skip]`, so future skip sites are covered without touching Rust (`8db64ed`) — **S** — DONE 2026-08-21
- [x] **W1.6** `pull_request` restored on `integration.yml`; also fixed `LUNARIS_MOON_URL` (the job set `MOON_URL`, which the suite does not read) (`2379795`) — **S** — DONE 2026-08-21
- [x] **W1.7** Investigate and clear the red `perf-gates` on main — **M** — DONE 2026-08-22
      `perf-gates` is green on main and has been since the per-CPU baseline fingerprint landed: GitHub's
      ubuntu pool is not one machine, `rerank/1x8` falls into two clusters 16.8% apart, and one shared
      baseline made the 5% cliff a coin flip. Keying the baseline by CPU model ended that.
      Residual, documented not fixed: on a CPU class with no blessed baseline the bench still runs but
      the gate is vacuously green, and the no-baseline branch saves a baseline it never uploads (upload
      fires only on `SAVE_BASELINE == 'true'`), so that hardware never self-blesses. Each CPU class needs
      one `workflow_dispatch` with `save-baseline=true`.
      **The gate that was actually broken was `recall-ratchet`, not `perf-gates`** — see below.
- [ ] **W1.8** Give the public SDK + HTTP surfaces the activation prior — **M**

> **Correction (2026-08-21) — the parked test would NOT have caught W1.1.**
> The plan said to un-ignore `uat4_forget_honors_scope_target_contract` because
> it was "the test that catches it". Mutation testing disproved that: revert the
> handler line and that test still **passes**. A `Scope::dev()` scan matches
> nothing, and "matched nothing" is exactly what the new 404 arm answers, so the
> cross-scope assertion is satisfied by a completely inert verb. The defect was
> never cross-tenant *leakage* — nothing was ever deleted for anybody. Only a
> test that asserts an **in-scope forget actually writes**
> (`uat4_forget_in_own_scope_actually_writes`) discriminates. Recorded in the
> test's doc comment so nobody deletes it as redundant.
>
> The general lesson, and the reason mutation-checking is mandatory on this
> plan: *a test parked against a bug is not automatically a test that detects
> the bug.* Four releases of confidence rested on one that did not.


## W2 — Truth

*Exit gate: every public claim is either measured and cited, or gone.*

- [x] **W2.1** Retract `p50 10.3 ms` at all 10 sites; replace with the GA-2b envelope — **S** — DONE 2026-08-21 (verified: zero live `10.3 ms` sites in README/POSITIONING/ARCHITECTURE)
- [x] **W2.2** Retract "millions"; state "sub-25 ms at 100k documents per scope, measured" — **S** — DONE 2026-08-21 (verified: no unqualified "millions"; README states the 100k envelope)
- [x] **W2.3** Retract "rerank sub-30 ms" and the "~12 ms budget"; publish the measured 1301 ms — **S** — DONE 2026-08-21 (verified: no live "sub-30 ms"/"12 ms budget" outside this ledger)
- [x] **W2.4** Retract "enforced in CI" for the latency contract — **S** — DONE 2026-08-21 (verified: `CLAUDE.md:49` now states the latency contract is NOT CI-enforced)
- [x] **W2.5** Retract the LoCoMo and ER-F1 gate claims from `CLAUDE.md` — **S** — DONE 2026-08-21 (verified: `CLAUDE.md:39-45` retracts the LoCoMo/ER-F1 gate claim and says why)
- [x] **W2.6** Fix `repository` → `pilotspace/lunaris`; republish — **S** — DONE 2026-08-21 (verified: `Cargo.toml:109` = `https://github.com/pilotspace/lunaris`)
- [x] **W2.7** README: add the v0.7.0 status row, "eleven tools" → 16, "three backends" → Moon, fix the garbled line 16 — **S** — DONE 2026-08-21 (verified: no "eleven tools"/"three backends" left in README)
- [x] **W2.8** The bare crates.io install form **does not work** — `lunaris-mcp`
  is `publish = false` and that command 404s. Replaced with the `--git` form at
  all 8 doc sites plus both postinstall fallbacks, each left with a warning
  naming the broken command. CI gate added that matches only *prescriptive*
  uses — a literal match would fire on those ten deliberate warnings and be red
  forever (`54d4bdb`) — **S** — DONE 2026-08-21
- [x] **W2.9** Purge dead Postgres / SQLite / JWT language from `CLAUDE.md`, the book, `configuration.md`; delete `LUNARIS_POSTGRES_URL` — **S** — DONE 2026-08-21 (`9818d46`, `34f9898`, `073a715`).
      What the parity tests covered before the purge: **nothing**. Both drivers gated every scenario on
      `LUNARIS_MOON_URL` **and** `LUNARIS_POSTGRES_URL`, and since `lunaris.open` rejects every scheme but
      `moon://` that gate could never open. The only thing executing was one offline check that
      `parity_golden.json` has the right schema_version, seed and five keys — a fixture-shape check. Zero engine
      coverage, inside `conformance-bindings.yml`, which pays for a live Moon on 6391 to run it.
      **TypeScript was worse than Python**: Python used `pytest.skip` (an honest SKIP); TS used a bare `return`,
      which vitest reports as **PASSED**. Measured across the real suite: 66 passed | 3 skipped → 55 passed | 14
      skipped — **11 TS tests were green while asserting nothing**. Two more files had the same defect outside
      the agent's boundary and are fixed in `073a715`: `backend_parity.spec.mts` (which carried the false premise
      verbatim in a comment) and `open_ingest_recall.spec.mts` (a silent bare `return`, so the most basic
      open→ingest→recall test in the SDK passed whenever Moon was unreachable).
      After: four documentary scenarios run for real on Moon in both SDKs, asserting min-hit counts, body needles,
      an exact 6-event count pinning the `.between` boundary, and unique `(source, id)` pairs proving RRF bucket
      isolation.
      `crates/lunaris-ts/__test__/documentary_parity.spec.mts` still probe `LUNARIS_POSTGRES_URL`, a backend
      deleted in 0.7.0. Those branches can never fire, so whatever they guarded is untested — the purge has to
      report what coverage actually remains, not just delete the dead code. `milestones/` mentions are history
      and stay.
- [x] **W2.10** Stop selling "bi-temporal" unqualified; scope it to the 1-hour live window or drop the crate keyword — **S** — DECIDED 2026-08-21: **keyword kept**.
      The prose is already scoped everywhere that matters — `README.md:257` spells out that as-of reads are
      search-side and graph-side while historical **KV** reads refuse rather than answering with today's data,
      and `CLAUDE.md:21` carries the same qualification. The crates.io keyword stays because bi-temporal is
      *real*: `BiTemporal { valid, sys }` is a required field on all six primitives and `forget`/supersession
      close intervals instead of destroying rows. Only the KV hydrate half is unavailable on Moon. Dropping the
      keyword would understate a genuine differentiator to avoid qualifying one sub-case, and a keyword has no
      room to carry the qualification anyway — that job belongs to the prose, which now does it.
- [x] **W2.11** Rewrite `quickstart-py` / `quickstart-ts` for Moon; add CI that compiles every example — **M** — DONE 2026-08-21 (`ef4c428` RED, `010c35c` GREEN).
      All seven examples rewritten for Moon. The RED commit is worth keeping: the gate alone scored **23**
      findings against the unfixed tree — every sample a new reader copies pointed at the Postgres backend
      deleted in 0.7.0, or at the `pg-lunaris` image deleted with it. The examples README already *admitted* the
      image was gone; the samples were never updated to match.
      `.github/workflows/examples.yml` now runs `cargo check --locked`, `tsc --noEmit`, and `mypy` + `import main`
      per example, with no `|| true`, no `continue-on-error`, and no skippable `if:`. Plus
      `examples/ci/no-dead-backends.sh`, a grep for the one thing a typechecker cannot read — a URL string in a
      README. Mutation-verified: reintroducing one `postgres://` line fails it, removing it passes.
- [x] **W2.12** Sync `RELEASES.md`; add `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, issue/PR templates, badges; refresh `deny.toml` rationale — **S** — DONE 2026-08-21 (verified: `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, `RELEASES.md` (0.7.0 present), 4 issue templates + PR template)
- [x] **W2.13** Document the ~40 operator-facing env vars, starting with `LUNARIS_ACTIVATION_BOOST` — **S** — DONE 2026-08-21 (verified: `configuration.md` documents 145 `LUNARIS_*` references incl. `LUNARIS_ACTIVATION_BOOST`)
- [ ] **W2.14** Publish or delete `lunaris-cli`; deprecate the candle-era crates still on crates.io — **S** — half done 2026-08-21: `lunaris-cli` is settled — `crates/lunaris-cli/Cargo.toml:34` is `publish = false`
      for the same structural reason as `lunaris-mcp` (path dep on `lunaris-memory-service`), and W0.9 covers
      distributing it as a release binary instead. Still open: deprecating the candle-era crates on crates.io,
      which is an **owner** action — deleted crates get tombstones, never yanks.

## W3 — Evidence

*Exit gate: every published number has a committed harness, committed raw
artifacts, a stated configuration and an error bar — and a stranger can re-run
it. Tencent cannot say this. That is the entire pitch.*

- [x] **W3.1** `85.4%` / `98.2%` / `93.0%` retracted with a dated banner; `J=96.0%` annotated as an N=50 subsample at all three sites (`89cff0c`) — **S** — DONE 2026-08-21
- [x] **W3.2** **DECIDED** — two named operating points: fast (rerank OFF) + quality (rerank ON). See Decisions.
- [ ] **W3.3** Re-run LME N=125 A/B on the committed harness at current HEAD — **M** — `OWNER-BLOCKED` (needs `MINIMAX_API_KEY`)
- [x] **W3.4** Raw-artifact convention + `scripts/bench/publish_raw.py`; `lme-raw/` and `pm-raw/` scaffolded GA-2b style (`5b915db`) — **S** — DONE 2026-08-21
- [~] **W3.5** Design specified in `docs/benchmarks/lift-and-token-reduction-design.md` (`72e19b1`); execution pending — **M**
- [~] **W3.6** Design specified alongside W3.5 (`72e19b1`); execution pending — **M**
- [~] **W3.7** Decision made — **ratchet both points, `fast` as the primary gate**. `offsets40.tsv` added (the old `offsets16.tsv` covered only **2 of 6** LongMemEval-S categories). BLOCKED on two N=40 measurement runs to bless the baselines; the `recall-ratchet.yml` edit must land *after* them (`8a6e5cf`) — **S**
- [x] **W3.8** Reader-ceiling claim amended, `suggest_new_ideas` marked degenerate, README fact-recall row no longer quotes the oracle arm beside a single-reader headline (`d65936b`, `7278473`) — **S** — DONE 2026-08-21
- [x] **W3.9** Cite or drop every competitor number — **S** — DONE 2026-08-21 (verified: no bare competitor percentages left in README)
- [x] **W3.10** **RETIRED as unsubstantiated**; the ±5-pt judge/generation noise floor, which IS evidenced, written up in `docs/benchmarks/measurement-noise.md` (`4a9251e`) — **S** — DONE 2026-08-21

## W4 — Reach

*Exit gate: Lunaris arrives inside a runtime people already use, and what it
remembers can be read by a human without a debugger.*

> **Correction (2026-08-21).** The plan originally described RAPTOR as costing
> "LLM and storage on every ingest". The LLM half is **wrong**:
> `crates/lunaris-ingest/src/pipeline.rs:524` hardcodes `ExtractiveSummarizer`
> ("no LLM required, always succeeds"), and `LlmSummarizer` is feature-gated
> with zero pipeline call sites. Measured cost when ON, per ingest, with
> `N = max(1, heading count)` communities: **0 LLM calls**, **+1 `embed_batch`**
> over N summaries, **+2N WriteOps**, **~3.1–7.4 KB per community**. The gate is
> still correct — a heading-free turn, which is what MCP and the hook actually
> ingest, yields `N = 1` and pays a full extra embedder round-trip to summarise
> text stored verbatim one key over — but the justification is embedder and
> storage cost, not inference cost.

- [ ] **W4.1** Package the Claude Code hooks + MCP path as *the* headline install — **M**
- [ ] **W4.2** Human-readable memory artifacts — a persona / scenario view — **M**
- [x] **W4.3a** `distill` accepts firsthand observations (`5b56ec7`) — **S** — DONE 2026-08-21
- [ ] **W4.3b** Direct-capture write path + the four capture kinds — **M**
- [ ] **W4.4** Demote raw telemetry to non-injected substrate — **M**
- [x] **W4.5** RAPTOR write gated behind `LUNARIS_RAPTOR_ENABLED`, default OFF (`391b76c`, `f80ea84`, branch `feat/w45-gate-raptor-write`) — **S** — DONE 2026-08-21
- [ ] **W4.6** D6 governance: thread scope through `Publisher::publish`; audit consumer, drop counter, retention — **L**
- [ ] **W4.7** Publish `lunaris-integrations`, or relabel it "reference adapters" — **M**
- [x] **W4.8** "Every writer has a reader" CI gate — **M** — DONE 2026-08-21.
      `scripts/tests/test_gated_tests_have_a_runner.py`, wired into `ci.yml`. It asks one question: does any
      `cargo test` in `.github/workflows/` enable the feature each `#![cfg(feature = "X")]` test file is gated on?
      A file gated on a feature nobody passes compiles to nothing, and `cargo test --workspace` walks past it and
      still reports green.

      **Five orphaned suites on the first run**, none found by eye:
      1. `lunaris-cli/tests/try_end_to_end.rs` (`embedded-moon`) — the three tests proving `lunaris try` actually
         starts a store and returns hits. The new front door had zero behavioural CI coverage. Fixed: dedicated job.
      2. `lunaris/tests/remote_llm_wired.rs` (`cloud-api`) — a built-vs-wired discriminator that counts requests a
         fake `/v1/chat/completions` receives; 0 means the resolver silently fell back to Noop. CLAUDE.md makes the
         extractor/verifier slots REMOTE-ONLY, so this guards the primary LLM path. Fixed: `ci.yml` step.
      3. `lunaris-llm/tests/openai_compat_fake_server.rs` (`cloud-api`) — same feature, same fix.
      4. `lunaris-ingest/tests/c_live_moon_embedding_kv_size.rs` (`moon-it`) — the byte-level proof that the KV
         embedding double-store fix survives the real path. `moon-it` *is* in CI, but only for three other crates,
         so per-crate matching was what caught it. Fixed: `integration.yml` step.
      5. `lunaris-bench/tests/budget_assertions.rs` + `lunaris-conformance/tests/sdk_embedder_parity.rs` —
         allowlisted with reasons; see W4.9 and W4.10.

      Two design calls carried the result. `cargo build` and `cargo bench` deliberately do **not** count as runners:
      both compile a gated file without executing an assertion, and `--features llamacpp` reaches these workflows
      through bench/build far more often than through test — counting a compile as a run would have hidden two of
      the five. And coverage is matched **per crate**, which is the only reason #4 surfaced. Both calls are
      mutation-proven: breaking the feature name resurfaces both `cloud-api` files, and downgrading one
      `cargo test` to `cargo build` resurfaces exactly that crate.

      The allowlist requires a reason >60 chars naming a tracking task, and a second test fails on entries that no
      longer gate anything — an allowlist that outlives its problem is how a gate stops meaning anything.
- [ ] **W4.9** Re-derive `budget_assertions.rs` rows for Moon-only, then wire `budget-it` into CI — **M** — NEW 2026-08-21
      Found by W4.8. This suite reads Criterion output and asserts the blueprint §4.1/§4.2 p50/p99 budgets — it is
      the implementation the **retracted** "latency contract enforced in CI" claim (W2.4) pointed at, and it has
      never run. Its 10 rows still name "Moon + Postgres" and Postgres was deleted in 0.7.0, so the assertions
      cannot be trusted as written. Re-derive the rows against Moon-only first; wiring it in before that would
      enforce a contract nobody has re-checked. Pairs with W1.7/S4 (the red `perf-gates`).
- [ ] **W4.10** Write a cross-SDK embedder parity test against `llamacpp()` — **M** — RESCOPED 2026-08-21
      The old `sdk_embedder_parity.rs` is **deleted**, along with the `sdk-parity-it` feature it was the sole
      user of. `EmbedderConfig.fastembed()` no longer exists in either SDK (confirmed: zero hits in
      `lunaris-py/src` and `lunaris-ts/src`), so the test's subject was gone — keeping 362 lines allowlisted
      forever is exactly the rubber stamp the allowlist rules exist to prevent.
      The *concept* is worth rebuilding: 100 fixed inputs through both SDKs, two 100×768 f32 matrices, asserted
      bits-exact. That catches codegen-vs-handwritten divergence, locale-dependent tokenisation and FFI float
      corruption — the family that produced the silent-zero-vectors P0. Rebuild it against `llamacpp()`, and fix
      the skip path while doing so: the old one returned `Ok(())` when an interpreter was missing, so even wired
      up it would have reported green having tested nothing.
      Deleting it proved the gate's own stale-entry test: with the file gone and the allowlist entry left in
      place, `test_allowlist_has_no_stale_entries` failed and named the entry.
      Found by W4.8. It drives both SDKs through `EmbedderConfig.fastembed()`, an API **retired** by the v0.6
      llama.cpp-only cutover, so it cannot pass as written. Rewrite against `llamacpp()` or delete it. Note it also
      returns `Ok(())` when an interpreter is missing — the same skip-passes-as-green shape as the `[skip]` sites —
      so that path needs fixing before it is worth wiring in, or it will report green having tested nothing.

---

## Decisions — settled 2026-08-21 (owner)

**Ship target: public OSS launch.** The Tencent framing. W0 (open the front
door), W2 (truth) and W3 (reproducible numbers) drive the order; the Helios
canary follows rather than leads. W1's two P0s remain a hard prerequisite
regardless — you cannot invite strangers to a `/v1/forget` that silently
no-ops.

**W3.2 — publish TWO NAMED OPERATING POINTS, not one.** A *fast path*
(rerank OFF, p50 ~20 ms, the shipped default) and a *quality path* (rerank ON,
~1301 ms p50). Both are real, both are measured, and naming them turns the
config split from a gotcha a competitor exploits into a tradeoff the buyer
chooses. Nothing already measured gets thrown away. Every table, doc and
benchmark row must now state WHICH operating point it describes — an unlabelled
number is the defect this decision exists to kill.

**W3.5 + W3.6 — lift headline, absolutes below.** Lead with lift on a fixed
agent harness and with token reduction, because that is ground we win and the
harness ships (Tencent's does not). Keep absolute LongMemEval / PersonaMem
scores as a secondary table for readers comparing against published Zep and
Mem0 figures. Both tables, in that order.

**W4.5 — gate the RAPTOR write behind a default-off flag.** Stops the LLM and
storage cost on every ingest immediately, keeps every line of code, and is
reversible the day a reader exists. Not deleted, not wired.

## The recall ratchet never gated the `quality` arm (2026-08-22)

Found while clearing W1.7. `perf-gates` was fine; `recall-ratchet` was not.

The two-arm N=40 ratchet (`5e37105`) has run **exactly once** — on the W4.16
merge push, run `32484079897`, commit `dc11319`. All four `quality` shards died
mid-question at the 70-minute ceiling, both fan-in jobs were `skipped`, and the
run concluded **`cancelled`** — which is not a red board.

This is the third road to the failure mode `eval_workflow_guard.rs` already
guards twice. STARTUP-01 was the first, the merge-train concurrency cancel
(`0662cf3`) the second, a job timeout the third — GitHub reports it with the
same word. The file's own comment had already named the shape: *"a cancelled
gate is indistinguishable from a passing one on a board with no required
checks."*

Measured from the run's own step timings and artifacts:

| arm | setup+build | measure | per question | outcome |
|---|---|---|---|---|
| `fast` | 7m07s | 46m02s / 10 q | 4.60 min | success, 53m total |
| `quality` | 8m02s | cut at 62m10s | ~12.4 min | **CANCELLED at 70m** |

All four `quality` shards completed exactly 5 of 10 questions and died inside
the 6th — not a flake, a structural impossibility. The 70 was derived for the
N=16 single-arm shape and survived `5e37105` untouched when N went to 40 **and**
a 2.7x-costlier second arm was added; the workflow comment still described the
old shape, so nothing in review contradicted it.

Two defects, both fixed in `c4a0726`:

1. **The budget.** `timeout-minutes` 70 -> 170, and now DERIVED —
   `the_shard_timeout_covers_the_work_the_matrix_asks_for` recomputes it from
   `offsets40.tsv` and the matrix. Change the manifest or the shard geometry
   and the guard catches the drift, not review.
2. **One arm's timeout voided the other arm's verdict.** `needs: measure` waits
   on the entire matrix, so four complete, green `fast` shards sat in artifact
   storage uncompared. `if: ${{ !cancelled() }}` on the fan-in lets each arm's
   own "expected 4 shard config files" assertion decide — that assertion was
   already correct, it simply never ran.

Both halves proven against the real artifacts from the cancelled run:

```
fast     RATCHET OK: any-gold 39/40 vs baseline 39/40 (tolerance 1, floor 38)
quality  exit 5 — FATAL: run is NOT FINAL (artifacts=24/40, ERR=4:
         [251, 266, 283, 289]) — a partial run can neither be gated nor blessed
```

So the `fast` arm was green all along and CI discarded the verdict. Worth
recording separately: that 39/40 baseline was blessed on **Darwin-arm64** and
reproduced **exactly** on ubuntu x86, same single miss (`134`) — a stronger
cross-platform result than the tolerance was designed to assume.

**MEASURED 2026-08-22 — run `32506983470`, all 10 jobs green.** The `quality`
arm produced its first verdict in the project's history, and the 170-minute
budget is what made it possible: the quality shards ran **116-120 minutes**,
comfortably past the old 70-minute timeout that had been cancelling them and
comfortably inside the new one. The fast shards ran 51-63 minutes.

| arm | J (any-gold, N=40) | wrong |
|---|---:|---|
| `fast` (rerank OFF, shipped default) | **97.5%** | `134` |
| `quality` (rerank ON) | **100.0%** | — |

Zero errored questions on either arm. The single `fast` miss is `134`, and
`quality` answers exactly that one correctly — so the cross-encoder pass buys
one question out of forty, cleanly attributable rather than lost in noise.
That is the evidence the two published operating points needed: `fast` is not
a degraded mode, and `quality` is not a rounding error. Both baselines are now
blessed from a real verdict rather than from a local run.

Note the `fast` result reproduces the Darwin-arm64 baseline **exactly**,
including which question misses — a third consecutive cross-platform match.

## Follow-ups surfaced by Wave 1

- [x] **F1** `ScopedLunaris::recall` bootstrap gap — `test_cross_scope_isolation`
  fails with `unknown index` against a never-written scope. Found while proving
  W0.5; looks like a real defect, not a test artifact. — **DONE 2026-08-22.**
  It was a real defect, and a bad one: Moon creates a scope's FT index lazily on
  first write, so a scope nothing has been ingested into has no index and
  `FT.SEARCH` answers `unknown index`. Every recall leg propagated it with `?`.
  **The very first thing a new agent does — ask what it remembers, before it has
  remembered anything — was an error.** That is the worst place for a bug to
  live: unreachable from any existing deployment, only from a new one, so it
  survives arbitrary production traffic untouched.
  RED on a live Moon: `recall_on_a_brand_new_scope_returns_no_hits_moon` →
  `Storage(Backend("moon hybrid_search: redis error: ResponseError: unknown
  index"))`. GREEN: 5/5 in `moon_parity.rs`.
  **The interesting part is what the fix found.** FOUR call sites had already
  grown a private workaround for this one condition, with THREE different
  predicates: `primitives::working_memory` and `lunaris-hook::context` matched
  `unknown index`/`Unknown index`; `operators::tree` matched only `Index name`;
  the LongMemEval harness had its own. So the condition reported in one of
  Moon's spellings sailed straight past the site matching another. Now one
  `lunaris_retrieve::missing_index` predicate covers all three spellings and
  every leg (vector, keyword, navigate + its fallback, tree, hybrid fusion)
  plus `working_memory` routes through it.
  **Deliberately NOT fixed in `lunaris-storage-moon`.** At the storage boundary a
  never-written scope's `lunaris_{scope}_chunks_idx` and a typo'd
  `lunaris_{scope}_unknown_table_idx` produce the identical Moon reply — 
  swallowing it there turns a misconfigured index name into silent empty
  results, and `keyword_search_rejects_unknown_index_on_moon` pins that contract
  on purpose. Up in `lunaris-retrieve` the index name came from the plan, not
  from caller input, so "absent" can only mean "nothing written yet".
  Unit tests assert the other half — connection-refused, diskfull, timeout and
  CrossSlot all still surface, because a helper that swallowed everything would
  pass a happy-path test and turn every outage into an empty recall.
- [x] **F2** `.top(n)` was inert in both SDKs — `_collapse_plan` walked
  pre-order so the leaf `k` overwrote it, leaving the README's `.top(5)` at
  `k = 30`. Fixed in `12a74af`; check whether the same pre-order collapse
  affects any other operator. — **CHECKED 2026-08-22.**
  **Answer to the literal question: no.** The walk is post-order in both SDKs
  and no operator is still mis-ordered. `.top()` beats the leaf `k`,
  `filter`/`as_of`/`query` do not contend for a field.
  **But the check found something worse than ordering**, measured by running
  the real collapse in both SDKs:

      Vector("chunks", 10).and_(Keyword.bm25("facts", 20)).top(5)
        -> {"index": "facts", "k": 5}       # ONE leg. Flip the operands and
                                            # it searches "chunks" instead —
                                            # the index is chosen by the order
                                            # the caller happened to type
      Vector("chunks", 30).and_(Graph.anchored([alice], 2)).top(5)
        -> {"index": "chunks", "k": 5}      # the traversal is simply gone

  `index` and `k` are single-valued in the collapsed plan, so a composed plan
  silently becomes a single-leg query and the SDK returns a plausible list of
  hits for a question the caller did not ask. Same family as the `.top(n)`
  bug, one level up: not a wrong number, a wrong query.
  Both SDKs now REFUSE what the collapse cannot carry — more than one
  retrieval leg, or a `graph`/`tree`/`navigate` operator — naming every leg in
  the message. A `fuse_rrf` over a single leg still passes: it is a genuine
  no-op, and a no-op is not a lie. Three twin tests per SDK, in the files
  `conformance-bindings` already runs.
  **`docs/MIGRATING-FROM-ZEP.md` was selling this.** Its graph-traversal
  section — the "why leave Zep" pitch — showed
  `.recall().graph(lunaris.Graph.anchored(...))`. That method **does not exist**
  on the Python builder (`AttributeError`), and the composable spelling via
  `.and_()` silently dropped the traversal. The same section credited "an AGE
  Cypher query on Postgres", a backend deleted in 0.7.0. Rewritten as a Rust
  example with an explicit note that the SDKs cannot run it yet.
  **Follow-up, filed as F14:** wiring the graph leg through the FFI is the
  actual fix; refusing is the honest interim.
- [ ] **F3** napi `index.d.ts` generation has been wrong for four versions.
  Worked around by moving the public entry (`lunaris.cjs` / `lunaris.d.ts`)
  because CI calls `npx napi build` directly, bypassing `npm run build` and any
  flags added there. The underlying generator defect is still open, and the CI
  invocation is the reason the obvious fix does not work.
- [ ] **F4** `crates/lunaris-ts/package.json` `main` / `types` / `exports` now
  point at the new entry names. Grep found nothing hardcoding the old ones, but
  re-check at merge and before any packaging-doc work.


- [x] **F5** `ingest_episode_graph_on` never wrote a community at all — the
  RAPTOR tree was already absent from one of the two ingest paths, with no
  observable consequence. Further evidence nothing reads it.
  **Closed 2026-08-23 — not a defect.** Checked rather than assumed: `raptor`
  is entirely a `lunaris-ingest` pipeline parameter
  (`pipeline.rs::ingest_episode_with_raptor`), and `crates/lunaris/src/ingest.rs`
  has ZERO mentions of it. The episode path never had a flag to ignore, so this
  is not two paths disagreeing — it is two different ingest surfaces, only one
  of which chunks documents. Consistent with
  `docs/decisions/2026-08-21-gate-raptor-community-write.md`; the entry is
  evidence FOR that gate, not an open bug.
- [x] **F6** The RAPTOR conformance parity suite was **passing vacuously** —
  it compared two empty trees, which are trivially field-equal. Fixed in
  `f80ea84`. This is a live instance of the exact defect class W4.8 exists to
  prevent, found *inside a test*, and it argues for pulling W4.8 forward.
  **Closed 2026-08-23** — the fix landed in `f80ea84`; only the checkbox was
  outstanding. Note for anyone routing the rest of this cluster: W4.8 as built
  is a FEATURE-GATE runner check (does any workflow pass the feature a
  `#![cfg(feature = "X")]` file is gated on?), not a data-flow one. It cannot
  see a writer-without-a-reader, so "include it in the W4.8 sweep" in F5/F7
  did not land anywhere.
- [x] **F7** `Chunk.parent_id` has no production reader either — same
  writer-without-reader shape as RAPTOR and the audit log. Include it in the
  W4.8 sweep.
  **Closed 2026-08-23 — same disposition as F5.** Confirmed by sweep:
  `parent_id` is read only inside `lunaris-ingest/src/chunker/raptor.rs`, for
  tree construction, and nowhere in retrieval. That is RAPTOR's own internal
  wiring, and RAPTOR's write is gated default-off by ADR — so a field with no
  retrieval reader is the decided state, not a defect. See the F6 note on why
  the W4.8 sweep could never have covered this.
- [x] **F8** `.tree(..)` DSL users must now opt in and re-ingest. The failure
  mode is silent empty results, not an error — worth a louder signal.
  **Closed 2026-08-23.** `crates/lunaris-retrieve/src/operators/tree.rs` logged
  the missing-communities-index case at `debug!`, which is off in every
  production configuration — so a `.tree(..)` recall against a scope ingested
  without RAPTOR returned flat results and said nothing at all. Raised to
  `warn!` with a message that names the cause and the remedy: RAPTOR is opt-in,
  it does not backfill, and the scope's documents need re-ingesting to build
  the tree. Returning empty is unchanged and still correct per F1 — the change
  is only that the operator now explains itself.
  Guard: `crates/lunaris-retrieve/tests/tree_missing_index_is_loud.rs`, which
  captures the subscriber's output through a `MakeWriter` and asserts on the
  rendered log rather than on the operator's return value. Its sibling test
  pins that a *present but empty* index stays quiet, so the guard fires on a
  real misconfiguration and not on every empty tree recall — a warning that
  appears on the happy path is one the reader learns to skip.
  **Mutation-checked after committing the fix** (per the standing lesson that
  `git checkout` cleanup reverts an uncommitted one): reverting `warn!` to
  `debug!` fails with `log=""` — nothing rendered at all — while the anti-noise
  sibling stays green, so the guard is keyed on the level, not on incidental
  text.

- [x] **F9** **The conformance protocol suite never ran.** — CLOSED 2026-08-22
  `crates/lunaris-conformance/tests/run_protocol_lunaris_server.rs` skips
  cleanly when `target/<profile>/lunaris-server` is absent, and no CI job ever
  built it — every MemoryProtocol verb, the D-21 hard-delete rail, auth and
  rate limiting were reporting success without a server ever being spawned.
  Now built and green; doing so exposed three latent defects (an unsatisfiable
  future-LSN snapshot request, a rate-burst with no headroom, and a forget
  probe that never seeded anything). Same defect class as W1.5 and F6.
  **Closed 2026-08-22** after checking the fix is durable rather than one-shot:
  `integration.yml` has an unconditional `cargo build -p lunaris-server
  --no-default-features` step ahead of the conformance step, and all three of
  the runner's skip gates (`MOON_URL` unreachable / binary absent / no
  `LISTENING_ON` within 10s) route through `skip::skip_or_fail`, which the same
  job's `LUNARIS_CONFORMANCE_STRICT=1` turns into a hard failure. So deleting
  the build step no longer restores the silent skip — it turns the suite red
  with "was the binary built?". `tests/no_silent_skip.rs` pins both halves.
- [x] **F10** `lunaris-storage-moon` has contradictory skip discipline inside
  one crate: with Moon unreachable, `episode_roundtrip` and `moon_client_smoke`
  fail loudly while `a_quant_ef_guardrails` silently SKIPs green. Same crate,
  same missing dependency, opposite signals. — DONE 2026-08-22 (`6f0bab8`)
  Twenty skip decisions across **eighteen** files now route through
  `tests/common::note_moon_unreachable`, which panics under the switch
  `integration.yml` already sets (`LUNARIS_CONFORMANCE_STRICT=1`) and still
  skips without it. **Four of the eighteen were not findable by the name this
  entry implies** — `keyword_bm25.rs` and `scope_isolation.rs` decide inline in
  a `match` arm, `list_scopes.rs` calls its helper `maybe_connect`, and
  `multishard_live.rs` skips on a raw redis handshake; the new guard's
  rogue-file sweep found them on its first RED. The last two hold the six
  `#[ignore = "requires live Moon"]` tests CI un-ignores on purpose, so the job
  went out of its way to run exactly the tests that would have skipped silently.
  `lunaris-ingest`'s one site got the same treatment (its step exists because
  W4.8 found the crate gated with no runner). Verified live, not by inspection:
  no Moon + strict off -> SKIP green; no Moon + strict on -> RED; live Moon +
  strict on -> 236 passed / 0 failed.
- [x] **F11** `moon_storage_conformance` is not idempotent — BOTH HALVES DONE 2026-08-22 — it fails on a
  reused Moon (`expected None at 'now' before any write`). CI is fine (fresh
  Moon per job); it bites any developer who runs it twice.
  **Wider than one suite** (2026-08-22): `lunaris-storage-moon` has the same
  shape. Against a pristine Moon it is 236/236; re-run against that same Moon
  and a large number of tests fail on leftover state. Cost me two false
  diagnoses in one session while verifying F10. Fixing it means unique scopes
  or a per-run prefix, not a FLUSH — the suite must not assume it owns the
  store.
  **Storage-suite half DONE 2026-08-22.** `suite_scope::SuiteScope` mints one
  partition per `run_full_storage_suite` invocation and is threaded through all
  eight suite functions and both seeders — a parameter, never a process-global,
  because a global would make the new test pass for the wrong reason.
  **A fresh scope alone would have fixed nothing**: Moon namespaces the vector
  leg (`ft_index_name(scope, kind)`) and the graph leg (`graph_key(scope)`) but
  writes `KvPut` keys **verbatim** — `atomic.rs::run_ops` documents that the
  caller is expected to have scope-prefixed them, and the conformance suite
  never did. So `SuiteScope` owns both halves: `scope()` for the namespaced
  legs, `key()` for the bytes Moon takes literally. That KV contract violation
  was the actual bug; the scope was the visible half.
  RED/GREEN on a live Moon (6390) that was already dirty from the F10 session:
  the new `tests/storage_suite_is_rerunnable.rs` runs the whole suite **twice in
  one process** — RED with `expected None at 'now' before any write, got
  Some(Row { key: b"conformance:read_as_of:k1", ... })` on the *second* call
  (the first-run wrapper proves invocation 1 was green), GREEN after, and
  `moon_storage_conformance` now passes on that same dirty store.
  Own test binary on purpose: cargo runs integration binaries one at a time but
  parallelises `#[test]`s *inside* one, so sharing a file would have the two
  invocations racing — the same collision wearing a different hat.
  **`lunaris-storage-moon` half DONE 2026-08-22 — and my earlier diagnosis of it
  was wrong, which is the more useful half of this entry.** The claim above,
  "re-running against the same Moon fails a large number of tests on leftover
  state", does not reproduce. Measured on a fresh single-shard Moon: 236/0 twice
  sequentially, and after the fix below 237/0 **three times in a row** in CI's
  exact shape (parallel, `--include-ignored`, `--no-fail-fast`) against one Moon
  accumulating state the whole time. The "~40 failures" I recorded were two
  environment faults wearing a correctness costume:
  - I started the test Moon with `--shards 0` (auto = one shard per core). The
    single-shard guard refused every connect, loudly and correctly (task #21).
  - The scratchpad lives on the boot volume, which is at 97%; Moon crossed its
    disk-full threshold and answered `MOONERR diskfull: writes paused` to every
    connect. Test Moons now live on `/Volumes/Games` (408 GiB free), not
    `/private/tmp`. Same family as the 2026-08-21 6381 stall.
  Read the failure text before believing a count — a wall of reds all saying the
  same infrastructure sentence is one fault, not forty. See
  [[bench-h2-watchdog-broken]] for the last time I filed an environment fault as
  a code defect.
  **One real bug did exist, and it is fixed.** `moon_client_smoke.rs` put all
  seven of its tests on `Scope::dev()`, so they shared one FT index. Its seeded
  embeddings are near-identical ramps that all score 1.0, and
  `vector_search_as_of_round_trip_via_moon` asks for the top **5** and asserts
  its own id is among them — arbitrary once the index holds more than five rows.
  Reproduced deterministically: green, green, **RED on the third consecutive
  run**. Also red under the parallel sweep on a many-core box while green on
  CI's smaller runner, i.e. it would have gone red the day CI got bigger
  machines. Each test now takes its own `test_scope(...)`; ten consecutive runs
  green where run three had been red.
  The behavioural fix is invisible — the suite is equally green the day someone
  reintroduces `Scope::dev()`, right up until the index outgrows the k — so it
  is pinned by `no_test_in_this_file_uses_the_shared_dev_scope`, a source scan
  with a vacuity floor, mutation-checked (put one `Scope::dev()` back → RED
  naming the line). The other 17 files keep `Scope::dev()`: they were measured
  clean across three full re-runs, and a top-k over a shared index is the shape
  that breaks, not the shared scope by itself.
- [ ] **F14** The minimal SDK FFI carries one retrieval leg, so the Python and
  TypeScript DSLs cannot express the composed plans their own parity tests
  build. `crates/lunaris-py/tests/test_dsl_parity.py::test_vector_and_keyword_fuse_top_shape`
  asserts `isinstance(plan, RetrievalBuilder)` and nothing else — the
  composition builds and then executes as something different, so the test is
  green while proving only that a constructor ran. Found while closing F2;
  the refusal landed there is the interim, wiring the leg is the fix.
- [x] **F15** `examples/ci/no-dead-backends.sh` scans **`examples/` only**, and
  its own rationale ("nothing type-checks a URL string or a shell block in a
  README") applies at least as strongly to `docs/`, which is where a new user
  actually reads. Measured 2026-08-22: widening the scan to `docs/` +
  `README.md` yields **62 non-exempt hits**, and a sample is genuinely stale
  live instruction, not history — `docs/guide.md:9` still says "Two backends
  ship Day-0", `docs/guide.md:105` opens a `postgres://` URL,
  `docs/bindings.md:152` tells the reader to "swap in
  `postgres://postgres:pass@localhost/lunaris`", and
  `docs/book/src/reference/api.md:47` lists `lunaris-storage-postgres` as a
  shipping crate. All four return `UnsupportedScheme` since 0.7.0.
  Two guard defects to fix at the same time: (a) the exempt regex is tested
  against the whole `grep -rn` line, which INCLUDES the path — so every file
  under `docs/migration/` is auto-exempt by its directory name, whatever it
  says; (b) `retired` is not in the exempt list although
  `docs/book/src/operations/backends.md` uses it correctly.
  — **DONE 2026-08-22** (`7cfa0e7` guard + URL-string docs, `c726d3d` prose).
  RED named 20 sites across 14 files; widening the pattern set to
  Lunaris-owned symbols (`PostgresStorage`, `PG_URL`, `pgmq`,
  `run_storage_postgres`, `run_as_of_parity`) surfaced 16 more, including two
  conformance docs whose CI block named `eval-gauntlet.yml` — a workflow that
  no longer exists — and two test targets that no longer exist either.
  GREEN at 74 mentions checked, 0 live instructions.
  Three things worth carrying forward:
  - **A bare `Postgres` is the wrong pattern, not a stricter one.** It cannot
    separate "Lunaris supports Postgres" (false) from "Zep runs on Postgres"
    (true, and the point of the comparison tables). A guard that flags both
    gets muzzled with escapes until it detects nothing. The symbol set is
    what a grep can decide; the 51 remaining prose claims were swept by hand
    in `c726d3d` and 28 of them were left alone as correct.
  - **The guard needed a vacuity floor.** Move the docs and the old shape
    would scan nothing and print OK forever — the exact defect class it
    exists to catch. Floor is 20 matched mentions; verified it fires by
    pointing the roots at a clean directory.
  - **Mutation-check AFTER committing the fix, not before.** Mutation 1
    appended a live instruction to `docs/guide.md` and cleaned up with
    `git checkout docs/guide.md`, which silently reverted three uncommitted
    fixes in that same file. The guard went green afterwards only because the
    planted line was gone too. Caught by re-grepping for the original
    sentence; it would otherwise have shipped as a green board over
    unfixed docs.
- [x] **F12** `crates/lunaris-server/tests/recall_graph_mode.rs:563` is dead —
  it needs `PG_URL` against a crate deleted in 0.7.0. Reason string added; the
  deletion is W2.9's owner's call. — **DELETED 2026-08-22.** Deleting rather
  than rewriting Moon-only, because there was nothing to rewrite: the test
  enabled the graph pipeline and immediately recalled **without seeding a
  corpus**, then asserted the two backends' hit sets were non-empty and
  overlapped. Against two live stores it asserted over whatever the operator's
  data happened to contain. Parity was the only thing it added, and parity has
  one arm now. The four surviving tests in the file (all `MockGraphStorage`)
  pass; the gap the deletion leaves is filed as F16 rather than left implied by
  an `#[ignore]`d husk, because a husk reads like coverage in a directory
  listing — which is how W1.1's P0 sat parked for four releases.

- [x] **F16** No live-Moon test asserts that a `mode=graph` recall returns hits
  from a graph corpus **this repo seeded** — **S** — NEW 2026-08-22, surfaced by
  the F12 deletion. **CLOSED 2026-08-22** (`b791e40` RED / `d1423e9` GREEN) —
  and the missing test was the small half of it: `mode=graph` could not return
  a hit AT ALL. Three defects, each necessary, none sufficient alone.

  1. `ingest_structured` wrote facts to KV and the vector index but put **no
     `Fact` node in the graph**, while `ingest.rs` did. `hydrate_mixed`
     resolves a candidate id as a chunk row or a fact row and NEVER an entity
     row, so a traversal that reaches only entities yields no hit.
  2. `ingest.rs`'s Fact node carried every property except **`id_hex`** — the
     one the retrieval Cypher selects. Measured live:

         headers=["id_hex", "id", "lbl"]
         row=[String("e3978876e0b27ee9…"), String("e3978876e0b27ee9…"), …]  entity
         row=[Null,                        String("01a0292f0a1bb481…"), …]  Fact

     `hex::decode("")` on that NULL yields an empty id and the candidate is
     dropped. The one node kind that hydrates was the one the query could not
     name.
  3. The HTTP handler minted `EntityId::from_name_and_type(token, "")`. Every
     stored id is `blake3("name::Type")` with a REAL type, so the anchor matched
     nothing — and `degraded` stayed **false**, so it looked healthy while
     answering from the semantic leg alone.

  Fixes: structured ingest mirrors the extraction fan-out (Fact + HAS_FACT +
  FACT_ABOUT); Fact nodes carry `id_hex`; the handler resolves names against the
  graph (`WHERE n.name = nm`) and anchors on what comes back, so an unrecognised
  name degrades honestly instead of anchoring on an impossible id.

  The NoopEmbedder is what makes the end-to-end assertion discriminating: with
  all-zero embeddings the F22 guard omits `vec`, nothing seeded is in the KNN
  index, and the vector half of `Vector ∧ Graph::anchored` contributes nothing —
  so any hit is graph-derived. Mutation-checked after commit; each fix reds the
  suite on its own. `mode_graph_composes_graph_operator_when_gate_passes`
  re-baselined from one `graph_traverse` call to two, asserting the order
  (resolve, then traverse).

  Still open, deliberately: **entity rows do not hydrate.** A traversal that
  lands on an entity is dropped by design, so facts are the only graph-reachable
  retrievable unit. That is defensible — facts carry the text — but it is a
  design choice nothing states, and it is why a fact-free corpus makes
  `mode=graph` look broken. All four remaining tests in `recall_graph_mode.rs` drive
  `MockGraphStorage`, so they pin the handler's composition and its 501 gate but
  never prove the graph leg reaches real `GRAPH.QUERY` rows. Wants a seeder
  (ingest with `graph_pipeline().enable()`, assert the entity/relation rows
  land) plus one `mode=graph` recall over it, in the `integration.yml` job that
  already has a Moon. Note the trap the deleted test fell into: **seed the
  corpus in the test.** A non-empty assertion over an operator's ambient store
  proves nothing about the code under test.

- [ ] **F17** `crates/lunaris-ts/generated.d.ts` and `crates/lunaris-ts/index.d.ts`
  disagree about the TypeScript method surface — **S** — NEW 2026-08-22. The
  codegen IR dump renders snake_case (`with_graph_pipeline`, `ingest_ticket`);
  the napi-generated declarations render camelCase (`withGraphPipeline`,
  `ingestTicket`), and camelCase is what the runtime actually exposes. Nothing
  reconciles the two, so `generated.d.ts` is a trap for anyone writing against
  the TS SDK — it is how `conversational_parity.spec.mts` was written against a
  surface that does not exist and still type-checked. Either regenerate
  `generated.d.ts` through napi's own naming, or stop shipping it as a `.d.ts`
  (rename to `.ir.json` / put it under `target/`) so nothing resolves against
  it. Same class as W4.14: an artifact that *looks* like the contract, is not,
  and nothing fails when they drift.

- [ ] **F18** Two more absolute-count assertions over shared, never-cleaned
  scopes may exist — **S** — NEW 2026-08-22. `9ee5bc4` fixed the three
  `backend_parity` drivers, which asserted "exactly 10 episodes under
  `lunaris:_dev_:episode:`" and only ever held because they were the only
  writers. The same shape — an exact count or an `assert_eq!` on a scan of a
  long-lived store — is worth one sweep across the live-Moon suites before the
  next Moon starts outliving more runs. Grep seed: `scan_kv_prefix`,
  `scan_range`, `--scan`, `len() ==` near a `_dev_` prefix. Rerun safety is a
  property of the assertion, not of the store. — **SWEPT 2026-08-22**, one
  round: no other absolute-count-over-a-shared-scope assertion found in the
  TS or Python binding suites (`scope.spec.mts` filters on a unique sentinel,
  `toggles.spec.mts` asserts a tuple shape). The sweep DID turn up a different
  defect of the same family, fixed in the same wave — see F19. Leaving F18
  open only because the Rust live-Moon suites were not swept.

- [x] **F19** Two live-Moon TS suites swallowed every storage error as a PASS
  — **S** — NEW + DONE 2026-08-22 (`eb59f95`), found by the F18 sweep. Every
  test in `open_ingest_recall.spec.mts` and `scope.spec.mts` wrapped its whole
  body in `try { ... } catch (err) { if (msg.startsWith("STORAGE:")) return;
  throw err; }`, and a bare `return` from a test body is a PASS to vitest. So
  an `ingest`, `snapshot`, `forget`, or cross-scope `recall` that failed with
  ANY storage error reported green — including the one test in the repo that
  proves agent A cannot read agent B's memories. Nine of ten tests took the
  same bare `return` on an unreachable Moon too; exactly one had been fixed.

  The leniency was not wrong, it was misplaced: a plain Redis without
  RediSearch genuinely cannot open a Lunaris handle, and that IS a missing
  prerequisite. So the tolerance now lives in one `openOrSkip(ctx, url)`
  helper per file that owns the probe and the handshake and nothing else;
  every assertion after the open must succeed or fail loudly. Both bail-outs
  name their reason.

  Only visible now because W4.14 made these suites actually run. A gate that
  no job satisfies and a gate that swallows its own failures produce the same
  green.
- [x] **F20** The TypeScript SDK cannot construct an `Hlc`, so `.between()` and
  `.as_of()` are unusable from TS — **M** — NEW 2026-08-22, P1 — **DONE
  2026-08-22** (`78c29f6` RED / `6db7b97` GREEN).

  Every generated conversion site lowered a JS value through
  `serde_json::from_value::<T>()`, and the value arrived having already lost
  its integer-ness. The cause turned out to be NARROWER and sharper than the
  original filing said. It is not "napi passes f64": napi 3.8.5's
  `impl FromNapiValue for serde_json::Number`
  (`src/bindgen_runtime/js_values/serde.rs`) preserves integer-ness only for
  values that fit in **u32 or i32** and sends everything else through
  `Number::from_f64`. So the boundary is `u32::MAX` = 4_294_967_295 —
  **every millisecond timestamp since 1970-02-19**.

  That is why it survived so long: in `{ wall_ms: 1736467200000, counter: 0,
  node_id: 0 }` the two small fields convert fine and only `wall_ms` breaks,
  so any test written with small numbers passes and proves nothing.

  Fixed by `lunaris_core::json_repair::restore_integral_numbers`, reached
  through a `from_js` seam in `crates/lunaris-ts/src/js_value.rs` that the
  emitter now uses at all 14 call sites (zero `serde_json::from_value` left in
  `generated_ts.rs`). The repair only touches integral, finite, in-range
  numbers; genuine fractions, `f64` fields, non-numeric leaves and
  out-of-range magnitudes are untouched — the last deliberately, so a value
  nobody wrote is still rejected rather than saturated into range. Python is
  byte-identical after the regenerate: pythonize preserves integer-ness.

  **The helper lives in `lunaris-core`, not next to the generated code,
  because `cargo test -p lunaris-ts --lib` cannot link** — napi's
  `_napi_delete_reference` is supplied by the Node host, not the Rust test
  harness. A unit test inside the SDK crate could never have run. That is a
  standing constraint on where SDK-side logic can be tested at all.

  Verified in both directions rather than assumed. The end-to-end guard
  `crates/lunaris-ts/__test__/hlc_bitemporal.spec.mts` (5 tests, real FFI,
  live Moon) was run against an `origin/main` build with a healthy Moon:
  exactly the 3 conversion tests fail, with the production error string. A
  second mutation — reverting `from_js` to a plain `serde_json::from_value`
  and rebuilding — reproduces the same 3 reds. The 2 that stay green in both
  runs are the boundary-arithmetic check and the malformed-input check, which
  are correctly insensitive to the repair.

  The suite asserts on the CALL, not on rows, so **F21 cannot mask it** — F20
  made the call throw, F21 only makes it return `[]`. It also covers the
  second independent lowering path, `CodeRepoMemory.ingestCommit`, whose
  `committerDateUnixMs` lowers into an `i64` and broke identically.

  **W4.13 is half-answered.** "SDK bi-temporal time-travel has no passing test
  anywhere" was two defects; this was one of them. F21 is the other, and
  W4.13 stays open until it lands.

  Post-fix TS board: 11/11 files, 73 passed, 1 expected fail (F21), 3 skipped.


- [x] **F21** `TimelineReconstruction` cannot ingest an event with a historical
  valid-time, so `.between()` reconstructs nothing — **M** — NEW 2026-08-22,
  P1. DONE 2026-08-22.

  **It was filed one layer too shallow.** The recipe was not dropping the
  caller's valid-time; the valid axis was **not caller-settable anywhere in
  Lunaris**. Every primitive constructor stamps `bt: BiTemporal::now(clock)`,
  putting BOTH axes on the ingest instant, and no production path called
  `BiTemporal::at` — the constructor that exists precisely to set them apart.
  `Episode::t_ref`, the caller's channel for "the real-world date of this
  content", reached exactly one consumer (the extraction prompt's
  `reference_time_iso`) and was dropped everywhere else. So
  `Filter::ValidTimeRange` answered "what did we WRITE in this window" for
  every caller of every API, and the store was mono-temporal with a spare
  field. Live consequence: `lunaris-hook/src/lib.rs:327` sets `t_ref` from the
  real event timestamp on the personal store, and it was discarded on every
  capture.

  **A third, independent hole surfaced while fixing it** — and only because
  the recipe test kept failing after the first two fixes landed. The
  **graph-OFF** ingest path (`lunaris-ingest/src/pipeline.rs`) never wrote a
  `valid_time_ms` field at all. That is the SHIPPED DEFAULT and the path every
  `DocumentCorpus`-backed recipe takes, so `Filter::ValidTimeRange` matched
  nothing for its primary callers regardless of what the axis said. The
  graph-ON twin in `lunaris/src/ingest.rs` has carried the field since Plan
  09.1-02; the two metadata blocks were never diffed against each other.

  Fixed in three layers:

  1. **Core** — `Episode::ground_valid_axis()` moves the valid axis to `t_ref`
     (`Hlc::from_utc`, pre-epoch clamped, counter/node_id zero since a
     real-world instant carries no causality). Called at every ingest entry
     point, including the `lunaris-ingest` ones that `lunaris-bench` and the
     recipes call directly. The **system** axis is deliberately untouched: an
     `as_of` system query that can be talked into claiming we knew something
     before we recorded it is not an audit trail.
  2. **Chunks** — `ChunkDraft::into_chunk_valid_from` makes every chunk
     inherit `episode.bt.valid.0`. One rule, no conditionals at the five call
     sites: a chunk is a slice of its episode's content, so it was true in the
     world when the episode was, never when the chunker ran.
  3. **Recipe** — `DocumentCorpus::VALID_TIME_KEY` (`"valid_time_unix_ms"`) is
     read into `t_ref`. Contract decision, made rather than escalated:
     `TimelineReconstruction` is capped at 6 public fns by its own RCPDOC-04
     test and `DocumentCorpus` is shared by five recipes, so a new typed
     method or a widened tuple ripples through all of them for one caller's
     benefit. The key is unprefixed — "event" is the timeline's vocabulary,
     not the corpus's — and reading it is ADDITIVE, so it round-trips and
     still works with `.filter()`. `as_i64` is strict: a float, a string date
     or a null is NOT coerced, because a corpus that quietly reinterprets
     caller data as a system-level axis is worse than one that ignores it.

  Evidence: `crates/lunaris/tests/valid_time_axis.rs` (4 tests, 3 RED before)
  and `crates/lunaris-recipes/tests/timeline_valid_time.rs` (3 tests, 2 RED
  before). Both carry a vacuity floor — an UNDATED episode must still stamp
  the ingest instant — so "the historical date is honoured" cannot be
  satisfied by an implementation that backdates indiscriminately or writes no
  `valid_time_ms` at all. One test pins the axes coming APART, which is the
  property that makes the store bi-temporal rather than mono-temporal with a
  spare field.

  The two parked SDK markers are GONE and their real assertions restored in
  the same commit: `xfail(strict=True)` in `test_documentary_parity.py` and
  `test.fails` in `documentary_parity.spec.mts`. Both specs also had to change
  the key they send from `event_valid_time_unix_ms` to `valid_time_unix_ms` —
  without that they would have stayed red and read as "still broken", which is
  the failure mode where a parked test quietly stops detecting anything.

  **A FOURTH defect, found only by running the SDK suites end-to-end.** With
  the axis carrying real dates, the TypeScript parity test went from
  `expected 0 to be 6` straight to `expected 7 to be 6` — an off-by-one at the
  window edge. `Filter::ValidTimeRange` is documented half-open
  `[after, before)` and FIVE render sites treated the upper bound as
  inclusive:

  1. `storage-moon::vector::render_knn_filter` (KNN prefilter)
  2. `storage-moon::vector::filter_matches` (client post-filter)
  3. `storage-moon::keyword::filter_to_moon` (BM25)
  4. `retrieve::fusion::filter_node_to_hybrid` (native hybrid fuse)
  5. `retrieve::operators::aggregate` (FT.AGGREGATE)

  `TemporalQuery::between`'s own doc-comment said `[after, before]`, which is
  very likely where the closed reading came from. All five now encode the
  upper bound as `hi - 1` — exact, not approximate, because `valid_time_ms` is
  only ever written from an integer `wall_ms`, so `[lo, hi-1]` closed and
  `[lo, hi)` half-open contain the same integers. Moon's `(`-prefix for
  exclusive bounds was the obvious alternative and is NOT usable — see **F26**.

  **The fifth site is the lesson.** A live-Moon suite pinned the vector and
  keyword legs and both went green while the SDK stayed red, because the
  `DocumentCorpus` recipes take NEITHER — they compose
  `Vector AND Keyword |> fuse_rrf` through Moon's native `HybridFilter`. A
  guard covering four of five sites reads exactly like a guard covering all
  five. `lunaris-retrieve/tests/valid_time_half_open_hybrid.rs` now covers it,
  and is mutation-checked: restoring the inclusive `max` reds it.

  **Proven end-to-end in BOTH SDKs**, against a live Moon, with the parked
  markers removed: TypeScript 5 passed / 1 skipped, Python 5 passed / 1
  skipped (the skip is the unrelated `HISTORICAL_KV_READS` gap in both). Use a
  FRESH store — reusing one leaves prior runs' events inside the window and
  turns the exact-count assertion into nonsense (7 became 14 on a second run
  against the same Moon).

  **Behaviour change worth flagging:** `forget --before T` filters on
  `bt.valid.0` (`forget.rs:499`), so it now means "forget content ABOUT times
  before T" rather than "forget rows WRITTEN before T". That is the correct
  reading of the valid axis, and it is a change.

  **F20 + F21 together are what W4.13 was really recording**: the bi-temporal
  recipe surface did not work in either SDK, for two independent reasons, and
  nothing ran the suites that would have said so until W4.14.

- [ ] **F22** A document Lunaris failed to embed outranks every real match,
  permanently — **M** — NEW 2026-08-22, P1. Under the `1/(1+d)` score a
  `NoopEmbedder` ZERO vector sits at L2 distance `||q|| = 1.0` from any unit
  query, while a genuine but weak match (cosine 0.29) sits at 1.19. The
  un-embedded row wins. Zero vectors are not neutral — they are a *better than
  average* match to everything.

  Not hypothetical, and not test-only: **the documented Tier-0 build is exactly
  this configuration.** `resolve_embedder` falls back to `NoopEmbedder` with a
  `tracing::warn!` when no GGUF is staged and no remote embedder is configured,
  and any store that ever ingested in that state carries rows that beat real
  matches forever — a later re-ingest with a working embedder does not fix the
  rows already written. Surfaced on run 32524873697, where the README Python
  quickstart answered "what does Alice like?" with five
  `Episode N: alpha beta gamma...` conformance fixtures (seeded zero-vector by
  the Rust driver, which had no `embed-remote`) instead of
  "Alice loves chocolate." — in insertion order, the signature of a scoring
  tie.

  Candidate fixes: refuse to index a zero vector at write time; or score it as
  worst-possible rather than by distance; or make the Tier-0 fallback a hard
  error behind an explicit opt-out — which is already ledger task **W0.7**, and
  this is the strongest argument yet for taking the hard-error side of it.
  CI is unblocked separately by giving `lunaris-conformance` an `embed-remote`
  passthrough so the fixture corpus is seeded with real vectors.

  **MEASURED, 2026-08-22 — this is live, not theoretical.** KNN 5 over a
  random unit query against a real 768-d chunks index:

  | row              | `__vec_score` (distance) | `1/(1+d)` |
  |------------------|--------------------------|-----------|
  | all-zero vector  | **1.0**                  | **0.500** |
  | real chunk       | 1.7649367                | 0.362     |
  | real chunk       | 1.8104194                | 0.356     |

  The un-embedded row ranks FIRST. And a row written WITHOUT a `vec` field is
  absent from `KNN 200` while still scoring 7.367490 on BM25 and still
  hydrating — so skipping the field costs the row nothing it could otherwise
  have had. `HSET`ing a real `vec` later brings it into KNN at distance ~0,
  which is exactly what the promotion worker's `VectorUpsert` does, so the
  skip is reversible by machinery already in the codebase.

  **Census of the live personal store** (Moon 6381, 1,412,669 keys),
  read-only, every `vec` unpacked as f32:

      sampled 1235 chunk rows -> zero=622  nonzero=613  no-vec-field=0
      ALL-ZERO SHARE: 50.4%

  Half the store. By row age (ULID timestamp) it is persistent rather than a
  backlog: 28-62% every day from 2026-07-20 through 2026-08-22. And 300 of 300
  sampled zero-vector rows carry NON-EMPTY text in their own `meta`, so the
  promotion path had everything it needed and never ran — filed as **F25**.

  **Write-side fix landed** (`f073481` RED / `0a2d043` GREEN): `atomic.rs`
  omits the `vec` field when `unindexable_reason` reports all-zero or
  non-finite. All seven vector writes in the workspace (3 in `ingest.rs`, 3 in
  `structured_ingest.rs`, 1 in `embed_promotion.rs`) route through
  `WriteOp::VectorUpsert`, so one guard at the Moon write boundary covers
  every producer. Mutation-checked after commit: reverting the body to the
  RED stub reds all 3 tests.

  Also corrected: `crates/lunaris/src/handle.rs` claimed "Vector recall
  returns empty rows until a GGUF is staged". FALSE — it returned the zero
  rows, ranked first. A comment asserting the intended behaviour while the
  code did the reverse is part of why this survived.

  **STILL OPEN, which is why this stays unchecked: the 622-in-1235 rows
  already written.** The guard stops new corruption and does nothing for
  existing rows; they need their `vec` field dropped or backfilled. W0.7
  (hard-error Tier-0) remains the other half of the argument.

  **RE-SCOPE 2026-08-22, after F25.** This entry concluded the promotion worker
  "had everything it needed and never ran". Wrong — it ran, and its work orders
  were being destroyed in transit by `MQ POP` (F25 / pilotspace/moon#652). That
  does not change the write-side guard, but it changes the repair:

  1. With F25 fixed, newly captured rows promote on their own. The zero-vector
     share should stop growing; confirm that before building anything.
  2. The stranded promotion events are NOT gone from the server — `XAUTOCLAIM`
     reaches PEL entries the MQ surface cannot (verified live). A recovery pass
     over `__lunaris_embed__` may replay much of the backlog with no chunk-row
     scan at all.
  3. Only what neither of those recovers needs a scan-and-backfill tool.

  Do (1) and (2) before sizing (3). The 50.4% census predates the fix and must
  not be used to size the repair.

  **Sizing measurement, 2026-08-22 (read-only, live Moon 6381).** The stranded
  backlog is real and large:

      topics=465  total_entries=252961  total_pending=111729

  111,729 promotion events sit in the `__mq_consumers` PEL across 465 scoped
  `__lunaris_embed__` topics — claimed by `MQ POP`, never returned to a client,
  never ACKed. That is the concrete size of what step (2) can replay, and it is
  the direct cause of the zero-vector share rather than a coincidence beside it.
  Measured with `XPENDING <topic> __mq_consumers` / `XLEN`; both are read-only,
  unlike `XAUTOCLAIM`, which takes PEL ownership.

  **Prerequisite for step (1), and it is not satisfied yet.** "Confirm the
  zero-vector share stops growing" can only be observed once a binary carrying
  BOTH fixes is the one writing. The installed hook at `~/.lunaris/bin/lunaris-hook`
  is dated 2026-08-19 02:24 — it predates F22 (`0a2d043`) and F25 (`3f48c46`).
  Until it is rebuilt from merged `main` and reinstalled, the live store keeps
  accumulating zero-vector rows and keeps stranding promotion events, and any
  "did it stop growing?" reading is measuring the old binary. Reinstalling onto
  the live personal store is an operator action, not a plan action: it is left
  to the owner, following the existing `lunaris-hook-<version>.bak-<date>`
  convention already in that directory.

  **Step (2) validated end-to-end on a scratch Moon (6396), not on 6381.**
  Eight events pushed to a topic created with the old `MAXDELIVERY 3`, drained
  with the worker's own `MQ POP ... COUNT 1` shape:

      push c1..c8           -> XLEN 8
      MQ POP COUNT 1        -> c1
      MQ POP COUNT 1        -> c5        <- c2,c3,c4 skipped, not returned
      MQ POP COUNT 5        -> (empty)   <- six of eight destroyed
      XPENDING              -> 8

  The gap is exactly `count + max_delivery_count` per call, as moon#652 says.
  Then:

      XAUTOCLAIM <topic> __mq_consumers recovery 0 0 COUNT 100
        -> all 8, payloads byte-intact

  So the replay path works and the bodies survive. Two constraints it imposes
  on the recovery tool: `XAUTOCLAIM` returns entries that were already
  processed (c1 and c5 come back too), so replay MUST be safe to run over
  already-promoted chunks — it is, since promotion terminates in a
  `VectorUpsert` that `HSET`s `vec`, but the tool should still ACK what it
  completes so a second pass shrinks. And `XAUTOCLAIM` takes PEL ownership, so
  unlike the census above it is not safe to point at 6381 exploratively.

  **Step (2) IMPLEMENTED** (`29da307` RED / `9b792a5` GREEN). Rather than a
  one-shot backfill tool, `subscribe` self-heals: it asserts `MAXDELIVERY 0` on
  its own topic (a consumer must not depend on a producer having run since the
  fix — attached to a damaged topic it was stranding its own backlog three at a
  time), then opens with a bounded `XAUTOCLAIM` sweep of the `__mq_consumers`
  PEL before polling for new work. Recovered entries go out ahead of new ones
  so a busy topic cannot starve its backlog, and each is ACKed as it is yielded
  so a second pass shrinks rather than repeats. Every deployment therefore
  recovers its own strand on next start; there is nothing to run by hand.

  The sweep only reclaims entries idle >= 30s (`LUNARIS_MQ_RECLAIM_IDLE_MS`).
  `subscribe` is not exclusive, and a live consumer's pop-to-ack window is
  sub-millisecond, so the threshold stops a second process tearing an in-flight
  message out of the first one's hands while leaving months-old strandeds
  trivially eligible.

  Mutation-checked after commit, both halves load-bearing and independently
  detected: disabling the sweep reds the recovery test at "2 of 6"; flipping the
  subscribe-side `MAXDELIVERY` back to 3 reds it at "5 of 6" AND reds the
  healthy-topic no-duplicate test at "1 of 4".

  What remains for F22 is only step (1) — confirming the zero-vector share stops
  growing — and whatever a post-replay census shows step (3) still owes.

  **Expect a burst on first start after this ships.** The sweep hands back
  everything it recovers, so the first hook start against the live store will
  push on the order of 111k promotion events through the embedder in one go.
  That is work that should already have happened and the queue drains at
  whatever rate inference allows, but it is not free and it is not quiet:
  budget for a long first run, and remember only one llama.cpp process may be
  live at a time. It is also the reason step (1) should be measured AFTER that
  drain rather than during it — a census taken mid-replay is measuring a moving
  target.

- [ ] **F23** Nothing verifies that the SHIPPED `generated.rs` matches the
  codegen snapshot — **S** — NEW 2026-08-22, P2. `lunaris-codegen --emit`
  writes ONLY to `crates/lunaris-codegen/snapshots/`, and `--check` diffs only
  those three paths (`codegen_managed_paths`). The files the SDKs actually
  compile — `crates/lunaris-ts/src/generated.rs`,
  `crates/lunaris-py/src/generated.rs`, `crates/lunaris-ts/generated.d.ts` —
  are copied across BY HAND, and nothing anywhere compares them.

  Found while landing F20: the TS pair were already out of sync on `main`
  (the shipped file is rustfmt'd, the snapshot is not — 246 differing lines),
  which is benign here but is exactly the shape that hides a real one. A
  contributor who edits the emitter, regenerates, and forgets the copy gets a
  green `parity-check` over an SDK that still contains the old code — and
  `parity-check`'s job name says the opposite.

  Two candidate fixes: (a) a test that compares snapshot to shipped modulo
  formatting — `prettyplease` and `syn` are already dependencies of
  `lunaris-codegen`, though not dev-dependencies, so they need promoting;
  or (b) better, make `--emit` write both, and add the shipped paths to
  `codegen_managed_paths` so `--check` covers them. (b) removes the failure
  mode instead of detecting it, but needs the emitter's output to be
  rustfmt-stable first or the member-scoped `cargo fmt --check` will fight it.

- [ ] **F24** `crates/lunaris-ts/index.d.ts` and `index.js` are committed build
  artifacts whose CONTENT depends on the feature flags of whoever last ran
  `napi build` — **S** — NEW 2026-08-22, P2. Building with
  `--features bindings-it` (which every local test workflow needs) adds
  `conformanceFixtureEpisodes`, `embedderConfigEmbedBatch` and `scanKvPrefix`
  to `index.d.ts`, and building `--no-default-features` drops whatever
  `llamacpp` contributes. Caught here only because the diff was reviewed
  before committing; there is no guard, so the test-only surface can be
  published into the npm type definitions by an ordinary contributor doing
  the ordinary thing.

  Closely related to **F17** (`generated.d.ts` and `index.d.ts` disagree):
  both are "a checked-in artifact nobody regenerates deterministically".
  Fix them together — either stop committing the napi-generated pair and
  build them in the release job, or add a CI step that rebuilds with the
  PRODUCTION feature set and fails on any diff.

- [x] **F25** The embed-promotion worker silently fails to promote about half
  of all captures — **M** — NEW 2026-08-22, P1. **ROOT-CAUSED AND FIXED
  2026-08-22** (PR #155). It is none of the three candidates originally listed.
  The messages never reach the worker.

  **`MQ POP <key> COUNT <n>` claims `n + max_delivery_count` entries and
  returns at most `n`.** The surplus is inserted into the consumer group's PEL
  with `last_delivered_id` advanced past it, is never handed to any client and
  is never ACKed. `read_group_new` reads only `>`, so the MQ command family can
  never see those entries again. `max_delivery_count` defaults to **3** and
  `queue.rs::subscribe` polls `COUNT 1`, so every poll against a backlog deeper
  than one destroyed up to three messages, silently. Measured live:

      MQ CREATE tG; MQ PUSH tG body g1..g4
      XINFO GROUPS tG    -> consumers 0, pending 0, last-delivered-id 0-0
      MQ POP tG COUNT 1  -> g1
      XINFO GROUPS tG    -> consumers 1, PENDING 4, last-delivered-id = TAIL

  A message pushed AFTERWARDS is delivered normally, which is why this
  presented as a stable partial loss rather than a dead queue, and why "28-62%
  every day for a month, never draining" fit it.

  Filed as **pilotspace/moon#652**, with a second defect found alongside:
  `MAXDELIVERY 1` dead-letters every message on its FIRST delivery, so it
  delivers nothing ever (`MQ DLQLEN` proves it: 2 dead-lettered, 0 delivered).

  **Validated against v0.8.6** (released 2026-08-22, newer than the 0.8.5 we
  test against): built from the release tag and re-ran both repros on the live
  binary — both reproduce. `src/shard/mq_exec.rs` and `src/command/mq.rs` are
  identical to the 0.8.5 sources at every line involved.

  **Fix**: `publish` creates topics with `MAXDELIVERY 0`, which disables DLQ
  routing and makes the claim exactly `count`. Verified green on both 0.8.5 and
  0.8.6. `MQ CREATE` updates an existing topic and publish runs on every send,
  so a topic damaged under the old default heals on its next publish. This is
  queue-wide — consolidate and verify ride the same adapter and were losing
  messages identically.

  Also landed: `promote_batch` returns a `PromotionOutcome` (every id lands in
  exactly one of promoted / missing / empty_text / bad_id / failed, logged at
  WARN whenever `promoted < requested`, unconditionally); a failing batch no
  longer `?`s out and discards every LATER batch; and `run_worker` no longer
  kills itself on one unparseable payload.

  **Note for whoever reads this next**: fixing that parse asymmetry ALONE made
  things worse — it turned "worker dies on a bad payload" into "worker spins on
  it forever", because the poison message sat at the head of a queue that would
  deliver nothing else. The live-MQ test is what caught it.

  **Not lost, only stranded**: the PEL entries are unreachable through the MQ
  surface but `XAUTOCLAIM` reaches them (verified live). A recovery pass over
  the existing 6381 backlog is possible — see F22.

  Trade accepted: no dead-lettering, and `queue_length` (which reports DLQ
  depth) now returns 0 unconditionally. Documented at the call site. The DLQ was
  not a working safety net anyway — it was precisely where the losses were NOT
  going.

- [ ] **F26** A KNN prefilter Moon cannot parse degrades to NO FILTER instead of
  an error — **M** — NEW 2026-08-22, P1, **Moon-side**. Found while fixing F21's
  boundary half.

  Moon has TWO numeric-range parsers and they disagree. The full FT.SEARCH
  grammar supports `(`-prefixed exclusive bounds and works correctly — verified
  live on a scratch index:

      @vt:[100 200]    -> probe:1, probe:2
      @vt:[100 (200]   -> probe:1
      @vt:[(100 300]   -> probe:2, probe:3

  The KNN prefilter (`{expr}=>[KNN k @vec $query]`) is parsed by a separate,
  smaller parser at
  `vendor/moon/src/command/vector_search/ft_search/parse.rs:290`, whose numeric
  branch is a bare `parts[1].parse::<f64>().ok()?`. On `"(200"` that yields
  `None` **for the whole filter**, and the caller falls through to an unfiltered
  search. Measured: a `@valid_time:[lo (hi]` prefilter returned 4 of 4 rows,
  ignoring the window entirely.

  **The parse gap is the small half; the silent degradation is the large one.**
  Any filter Lunaris renders that this parser cannot read returns MORE rows than
  asked for, with no error, no warning and no metric. A filter that silently
  stops filtering is indistinguishable from one that matched everything — and on
  a scoped store, "returns more than asked for" is one schema drift away from a
  confidentiality bug rather than a recall bug.

  Worked around on the Lunaris side rather than blocked on: both render paths now
  encode the exclusive upper bound as `hi - 1` rather than `(hi`. That is exact,
  not an approximation, because `valid_time_ms` is only ever written from an
  integer `wall_ms` — the closed range `[lo, hi-1]` and the half-open `[lo, hi)`
  contain the same integers. See F21. It makes Lunaris correct today and does
  nothing about the hazard for the next filter anyone adds.

  Owner action, both halves: teach the KNN prefilter parser `(`-prefixed bounds
  so the two parsers agree, and — more importantly — make an unparseable
  prefilter an ERROR rather than a silent pass-through. Filed as
  **pilotspace/moon#648**; not fixable from this repo (`vendor/moon` is
  read-only here).

  **Validated against v0.8.6** (released 2026-08-22): `ft_search/parse.rs` is
  byte-identical to the 0.8.5 file, and the defect reproduces on a live v0.8.6
  binary built from the release tag. Tighter repro than the original — it
  isolates BOTH bounds and carries an unfiltered baseline, so the over-large
  result cannot be waved away as a filter that legitimately matched everything:

      @vt:[100 400]   (plain FT.SEARCH)   -> 3   correct
      @vt:[100 (250]  (plain FT.SEARCH)   -> 1   correct
      *=>[KNN 10]                         -> 4   unfiltered baseline
      @vt:[100 400]=>[KNN 10]             -> 3   inclusive parses fine
      @vt:[100 (250]=>[KNN 10]            -> 4   FILTER SILENTLY DROPPED
      @vt:[(150 400]=>[KNN 10]            -> 4   FILTER SILENTLY DROPPED
      @vt:[100 249]=>[KNN 10]             -> 1   the hi-1 workaround holds

  Exact site is `parse.rs:291-292`, not 290 as first written.

  **Lunaris-side containment landed 2026-08-23.** The upstream half is still
  open and still owner/Moon-side, but the residual hazard the entry named —
  "does nothing about the hazard for the next filter anyone adds" — is now
  covered. `crates/lunaris-storage-moon/tests/knn_prefilter_is_never_silently_dropped.rs`
  asserts that EVERY filter `render_knn_filter` returns `Some(_)` for actually
  constrained a live Moon, by exact row set against an unfiltered baseline
  seeded in the same scope. The corpus splits 2/2 on each of two axes, so the
  `And` case expects one row — strictly smaller than either conjunct alone —
  which separates a half-applied filter from a wholly-dropped one. Mutation-
  checked after commit: reinstating `(hi` reddens the valid_time and And cases
  while the baseline and source cases stay green; making `And` keep only its
  first conjunct reddens the And case alone.

- [ ] **F27** **The silent-skip guard was itself keyed on one family's
  wording** — **M** — NEW 2026-08-23, P1. Found while writing F26's guard: the
  new live test tripped nothing, and it should have.

  `crates/lunaris-storage-moon/tests/no_silent_moon_skip.rs` exists to catch a
  test file that skips a live-Moon suite without routing through
  `common::note_moon_unreachable`. Its rogue sweep required the literal phrase
  `"not reachable"` alongside `SKIP` — which matches the `connect_or_skip`
  family and nothing else. Six files reach Moon through
  `EphemeralMoon::spawn()` and announce `no ephemeral Moon (..); SKIP`, so the
  sweep was blind to every one of them.

  Two of the six were **the F21 and F22 guards**. Both run in the strict
  integration job, which sets `LUNARIS_CONFORMANCE_STRICT=1` at job level
  precisely so a suite cannot report success without reaching Moon — and both
  would have gone green on a Moon that never came up. `dim_configurable.rs`,
  `mq_backlog_delivery.rs` and `mq_stranded_recovery.rs` were the others.

  Fixed in the same commit: the sweep now keys on the decision's observable
  form — a print announcing a skip — rather than on one family's wording, and
  all six route through the helper and sit on ROSTER. Bare `SKIP` was tried
  first and over-matched `multishard_failfast.rs`, which carries "must be
  SKIPPED entirely" inside an `assert!` message; an assertion is a failure, not
  a skip.

  Verified behaviourally, not structurally: with `MOON_TEST_BINARY` pointing at
  a path that does not exist and strict mode on, `valid_time_half_open` now
  fails 2/2 where it previously passed 2/2.

  **Generalisation landed 2026-08-23.** The first write-up of this entry
  guessed at the blast radius; a proper sweep gave the real one. Two crates had
  a guard and BOTH were blind — `lunaris-conformance`'s required
  `eprintln!("SKIP` with `SKIP` immediately after the quote, while every real
  site there writes `eprintln!("run_storage_moon: SKIP ..)`, so it has never
  matched anything. Unrouted skips existed in `lunaris-conformance` (4 files),
  `lunaris-server`, `lunaris-ingest`, `lunaris-retrieve` and
  `lunaris-storage-moon`. `lunaris-recipes` genuinely has none.

  Fixed at the CAUSE rather than with a third guard:
  `lunaris_test_harness::strict_skip` is now the one decision, replacing the
  fourth private copy (`lunaris-ingest`'s). It lives in `src/`, not a
  test-local `common/`, which is what makes the sweep exact — the SKIP print is
  outside every `tests/` tree, so "a test file that prints a skip decided it
  alone" needs no carve-outs. 15 sites routed.
  `crates/lunaris-test-harness/tests/no_silent_skip_workspace.rs` is the
  workspace backstop, keyed per-SITE: both existing guards ask only whether a
  file MENTIONS its helper somewhere, which `run_storage_moon.rs` satisfied
  while printing two unrouted skips of its own.

  **Worth recording how the fix went wrong three times**, because each failure
  was the very defect the guard exists to catch. The sweep was rewritten three
  times: a `grep` whose `[^;]*` stopped at the `;` inside
  `"..MOON_TEST_BINARY); SKIP"`; a per-LINE check that missed two files
  spreading `eprintln!(` and its string across two lines; and a
  stop-at-first-`;` scan that missed `"MOON_URL unset; SKIP"`, where the
  delimiter was inside the string. Each pass found MORE offenders than the
  last — 6, then 11, then 13. **The count rising after a fix is the tell that
  the previous key was narrow, not that new debt appeared.** It now walks the
  macro invocation to its matching close paren with a string-aware scan.

  The reusable lesson: **a guard keyed on one family's phrasing is blind to the
  next family.** Match the token structure, not a delimiter guess.

  **Still open:** `UNROUTED_DEBT` in the workspace guard lists 7 files in crates
  the strict job does not run (`lunaris-retrieve`, `lunaris`). Harmless while
  nothing promises them a Moon; the list is a shrink-only ratchet so it cannot
  grow while it is paid down. `lunaris-storage-moon/tests/common/mod.rs` should
  also converge onto the shared helper — deliberately not done in the same
  commit, since those files were in flight on the F26 PR.


- [ ] **F28** **`keyword_bm25` has been passing on cross-step pollution** —
  **S** — NEW 2026-08-23, P2. Found while routing F27: the suite was skipping
  locally, so running it for real was the first time anyone had.

  Three defects in one file, all of which a green tick had been hiding:

  1. `url()` was `Option<String>` built as `.ok().or_else(|| Some(default))` —
     which can never be `None`. All three `let Some(u) = url() else { ..skip.. }`
     arms were unreachable: three skip branches that read like coverage of the
     unset case and could not fire. De-Optioned; the dead arms are deleted
     rather than routed, because routing dead code would have preserved the
     illusion.
  2. `keyword_search_escapes_ft_specials_on_moon` asserted nothing about
     escaping. It searched a `chunks` index no test in the file creates, so on
     a fresh Moon it fails with `no such index` (reproduced 3/3) and on a warm
     one it passes for a reason unrelated to `ft_escape` (3/3). It now seeds
     its own row; mutation-checked after commit by deleting the seed, which
     reproduces `no such index` on a fresh Moon.
  3. **Why CI never saw it.** `integration.yml` sets `MOON_URL` to the job's
     shared 6390 Moon, and earlier steps in the same job create `chunks` before
     `-p lunaris-storage-moon` runs. The suite has been green on state another
     step left behind.

  Fixed in the F27 commit. **Left open** for the generalisation, which is the
  part that matters: every suite pointed at the job-shared 6390 Moon can pass
  on a predecessor's writes, and nothing checks for it. The isolation the
  `EphemeralMoon` suites get by construction is exactly what the `MOON_URL`
  suites lack. Worth an audit of which files use which, and why.

- [x] **F29** **Five tests parked on a precondition CI already provides** —
  **S** — NEW/DONE 2026-08-23. Found while auditing F28.

  `coding_session_memory_smoke` reported "8 passed; 3 ignored" with `MOON_URL`
  SET. Five `#[ignore]`s in `lunaris-memory` name `MOON_URL` — which
  `integration.yml` exports at job level — but no step un-ignores them: the
  `lunaris-memory` step is scoped `--test moon_parity`. W1.1 fixed exactly this
  shape for `lunaris-storage-moon` and only that crate. Three of the five also
  named `PG_URL`, deleted in 0.7.0, so they read as twice as satisfiable as they
  were.

  **`ci.yml`'s ignored-test ratchet does not catch this and is not meant to.**
  It enforces that every ignore carries a REASON and that the COUNT is pinned.
  Both were true of these five the whole time. A reason string is a promise
  about what would un-ignore a test; nothing checked whether CI was already
  keeping it.

  Guard: `scripts/tests/test_ignored_live_moon_tests_have_a_runner.py` — the
  third layer of one question, after W4.8 (does any job pass the feature a
  `#![cfg(feature)]` file is gated on?) and W4.14 (does any job both set the
  Moon URL and run the SDK binding file?). Picked up automatically by `ci.yml`'s
  `scripts/tests/test_*.py` glob. Two vacuity floors, one asserting the
  step-scanner can SEE the storage-moon step — without it "no orphans" would
  pass by finding no runners at all. Step matching is per-STEP, not per-file, so
  the storage-moon step's `--include-ignored` cannot vouch for another package.

  **The mutation check caught the guard shipping blind**, which is the part
  worth recording. Removing `--include-ignored` from the storage-moon step left
  the guard GREEN: that step's own comment block explains what the flag is for,
  so scanning raw YAML found the flag in PROSE and counted the step as a runner
  after the real flag was gone. Prose about a thing satisfying a presence check
  for the thing — the fourth instance of that shape this week, and the reason
  `no_silent_moon_skip.rs` strips comments before scanning. Fixed by stripping
  comment lines; both mutations (per step, independently) now fail, and the
  vacuity floor fires with its own message rather than a generic one.

  Fixed: reasons reworded, and the `lunaris-memory` step extended with both
  files plus `--include-ignored`. Cost measured before wiring rather than
  assumed — 7s and 22s; the exact CI invocation runs 23 tests, **0 ignored**, in
  ~24s, well inside the 8-minute budget. `chaos_helios_sigkill` stays parked
  with its reason corrected to name the real blocker (50x2 SIGKILL wall-clock,
  not the environment). `EXPECTED_IGNORED` stays 12 — nothing was un-ignored at
  the source, only run.

- [x] **F13** One bare `#[ignore]` survives outside the new ratchet's scope:
  `crates/lunaris-ingest/src/chunker/token.rs:171`. Add the reason string
  rather than widening the ratchet — in-crate units sit beside their code,
  while an integration file can park a whole contract. — DONE 2026-08-22
  (`0b84f83`). Surfaced for real while routing F10: under `--include-ignored`
  it fails with a bare `NotPresent` and reads like a regression.

## Wave 3 — serialized, needs inference

Only ONE llama.cpp process may exist machine-wide, so these run one at a time,
never alongside a building agent.

- [~] **S1** `fast` arm MEASURED 2026-08-21: **39/40 (97.5%)**, zero ERR, one miss
  (q134, `single-session-preference`). Per category: multi-session 11/11 ·
  temporal-reasoning 11/11 · knowledge-update 6/6 · single-session-user 6/6 ·
  single-session-assistant 4/4 · single-session-preference 1/2. Cost n=40
  median 142 s, total 95 min. Written to `target/lme/staging-fast-n40.json`,
  **deliberately not into `baselines/`** — it gets blessed together with S2 so
  the guard's two-arm invariant is never briefly half-satisfied.
  *Finding:* the four categories `offsets16.tsv` never covered all score
  perfectly and the only miss is in a category it DID cover — so closing
  coverage defect (b2) exposed no hidden weakness. The justification is
  unchanged and still holds: a *future* regression across 27.5% of the
  manifest would have been invisible at any N. On re-reading,
  `baselines/README.md` did NOT imply a discovery had been made — its (b2)
  passage is correctly forward-looking. It needed the measured result added,
  not a rewrite; done in S2's commit.
- [x] **S2** `quality` arm MEASURED 2026-08-21: **40/40 (100%)**, zero misses,
  every category clean. Both files blessed together into `baselines/`.
  **The arms differ by exactly one question** — q134
  (`single-session-preference`) misses under `fast`, hits under `quality`.
  That is a real reranking effect, not a tie-break artifact: `pool` is per
  retrieval arm, so the fused set holds up to `2 x pool = 80` candidates,
  `hybrid_rerank_top_in` widens the cross-encoder window to exactly 80, and
  `topk=60` keeps 60 — reordering discards 20 of 80 and so changes set
  membership.
  *Cost, answering the open question in `baselines/README.md`:* `fast` median
  141.5 s vs `quality` median 163.5 s = the cross-encoder costs **~15%**, not
  a factor. Reported as a ratio, not as a correction to the 125 s CI figure —
  that came from the reference runner and this did not.
  *Run survived a mid-run infrastructure failure.* `/Volumes/Games` is an
  external USB volume and it self-unmounted at ~16:40, killing the gate at
  29/40. The two-consecutive-ERR guard aborted rather than burning the rest
  of the manifest, `done_anygold` (JSON **and** log) refused to resume-skip
  the half-written `q361.log`, and q361 re-ran and PASSED — so the partial
  never banked a phantom miss. See [[lunaris-benchmarks]].
- [x] **S3** DONE 2026-08-21 (`1d67105` RED, `6996269` bless, `f95367d` workflow).
  RED guard: the coherence check validated
  exactly ONE baseline, so the two files that will actually gate CI would have
  become the only unguarded ones. Now covers both, plus a 5.0-point
  sensitivity bar (`total >= 20 * (tolerance + 1)`, integer form), an
  operating-point/filename agreement check, and re-derivation of
  `operating_point` + `smallest_detectable_regression_points` (tally.py writes
  both, but they are hand-editable JSON). Verified RED-for-missing-files, GREEN
  on a fixture pair, and RED under three mutations — including corrupting the
  **second** list entry, since a loop reading only element zero passes a
  first-entry mutation by accident. The `recall-ratchet.yml` + docs patch is
  written and dry-run validated; it applied cleanly after S1+S2 were blessed.
  `f95367d` then made CI match: measure is a 4-shard x 2-point matrix (8 jobs),
  the ratchet job is a matrix over point, each arm merges only its own shard
  artifacts and gates against its own baseline, and the expected question count
  is derived from `offsets40.tsv` rather than hardcoded. New guard
  `workflow_gates_exactly_the_baselines_this_guard_validates` expands the
  workflow's `point` axes through the baseline path template and compares to
  `GATING_BASELINES`, so CI and the guard cannot disagree about which arms are
  gated. Mutation-verified for real this time (earlier rounds were simulated in
  Python, which proves the logic and not the compiled test): dropping the
  ratchet axis, narrowing it to `[quality]`, renaming a point, and removing the
  path template each go RED; green on restore, workflow byte-identical after.
  *An earlier draft of that assertion was `contains(template) || contains(name)`
  inside a loop — true for both names whenever the template is present, i.e. it
  verified nothing. Caught and replaced before commit.*
- [x] **S4** W1.7 DONE 2026-08-21 (`6685fa9`) — the red `perf-gates` was a
  HARDWARE lottery, not a regression. Absolute `rerank/1x8` times cluster
  bimodally: passes 1.2943-1.3127 s, failures 1.5160-1.5283 s — 1.4%/0.8%
  spread *within* clusters against **16.8%** *between* them. The blessed
  baseline sits in the fast cluster, so every push scheduled onto the slow one
  failed. Not noise (it reproduces), not code (the blamed commit adds a CLI
  crate and its `Cargo.lock` diff has zero deletions), not the runner image (a
  failure shared `20260810.271.1` with two passes). Fixed by folding a
  `/proc/cpuinfo` fingerprint into `BASELINE_NAME` — a 5% cliff is only
  meaningful against the 1.4% same-hardware spread. `BASELINE_NAME` had to move
  OUT of the job env (STARTUP-01: job-level `env:` is expanded before a runner
  exists) into a step that precedes the cache restore, since `$GITHUB_ENV` only
  reaches LATER steps.
  *Second defect found in passing:* the no-baseline branch benches with
  `--save-baseline` but the upload step only fires on `SAVE_BASELINE == 'true'`,
  so unseen hardware measures a baseline and throws it away — new CPU classes
  could never self-bless.
  *Rollout:* each CPU class needs one `workflow_dispatch` with
  `save-baseline=true` while scheduled on it. Until then the gate is vacuously
  green on unblessed hardware — honest, and strictly better than a false red.
  *Correction to my own earlier note:* I first filed "every PR run reports
  `skipped`" as an unfixed problem. It is not a defect — PR runs are
  deliberately opt-in via the `perf-bench` label
  (`|| contains(github.event.pull_request.labels.*.name, 'perf-bench')`),
  documented in the workflow header because the bench costs a llama.cpp
  compile plus ~700 MB of GGUF download. Do NOT "fix" it into running on every
  PR. The real consequence is narrower and still worth knowing: an unlabelled
  PR gives you no perf signal, so a regression lands on `main` before anything
  measures it — label the PR when touching `crates/lunaris-llamacpp/**` or the
  hot-path bench.
- [ ] **S5** W3.3 — LME N=125 A/B, both operating points (`OWNER-BLOCKED`: `MINIMAX_API_KEY`)

## Pulled forward

- [x] **W4.8** "Every writer has a reader" CI gate — moved out of the W4 tail. **DONE** — see the W4 entry above.
  It has now justified itself six times in one day: the parked `/v1/forget`
  UAT, the `[skip]` discriminators, RAPTOR's vacuous parity suite, the
  conformance protocol suite that never ran, `integration.yml` setting
  `MOON_URL` where the suite reads `LUNARIS_MOON_URL`, and W0.4's quickstart
  advertising release binaries that no workflow builds (W0.9). Every one is the
  same shape — a check, or a promise, that nothing produces is
  indistinguishable from one that is satisfied.

  The sixth widens the gate's scope in a way worth writing down: five of these
  were a test with no subject, but the last was a *document* with no producer.
  The same query answers both — for each claimed artifact, name the thing that
  emits it — so the gate should read docs for install/download instructions and
  check that some workflow actually builds what they hand the reader.

- [x] **W4.11** Pin `llama-cpp-sys-2` — **S** — DONE 2026-08-21 (`7a8b804`). Found by the W2.11 agent.
      The exact pin on `llama-cpp-2` does **not** constrain its sys sibling: 0.1.151 declares
      `llama-cpp-sys-2 = "^0.1.151"` (verified against the crates.io dependencies API), so any resolve without our
      committed lockfile takes the newest 0.1.x — 0.1.154 today. The lockfile hid it perfectly: everyone in this
      repo gets .151 and sees nothing, while `cargo add lunaris-memory` resolves fresh and does not. A defect
      reachable only by the people we are trying to reach. Upstream ships the pair in lockstep (every version
      .146–.154 has an exact match), so they only test the matched pair; that alone justifies the pin regardless of
      which symbol moved. Nothing in the crate calls it — it is declared purely to constrain the version, and stays
      optional + feature-tied so a Tier-0 build pulls no C++ toolchain.
- [ ] **W4.12** No scope-bound DSL path in either SDK — **M** — NEW 2026-08-21. Found by the W2.11 agent; I traced it.
      Both SDKs ship a codegen-frozen native `RetrievalBuilder` (compose-only: `and`/`fuse_rrf`/`top`/`filter`/
      `as_of`, **no terminal op**) alongside a real hand-written one. Python's lives in
      `crates/lunaris-py/python/lunaris/dsl.py` with a working `.execute()`; TypeScript's in
      `crates/lunaris-ts/lunaris.cjs`. Each SDK then patches the entry point to hand out the working builder —
      `_attach_recall_shim` in Python, `lunaris.cjs:293` for `Lunaris.prototype.recall`.

      **Neither patches the scoped handle's `dsl()`.** `lunaris.cjs` patches `Lunaris.prototype.scoped` (:59) and
      `Lunaris.prototype.recall` (:293), and stops there, so `scoped.dsl()` still returns the frozen builder while
      `index.d.ts:368` types it as `RetrievalBuilder` — it typechecks against the shadow and fails at runtime.
      Python's `crates/lunaris-py/src/scope.rs:236` does the same.

      Severity, stated accurately: the frozen methods raise `NotImplementedError` with a message naming the working
      class, so this is a **loud** dead end, not silent breakage. But the substantive gap is real and wider than the
      method — `dsl.py` has no scope support anywhere (the only `scope` match in the file is an unrelated
      `__slots__` comment), so **a caller composing a DSL query cannot bind it to a partition by any route**. For a
      product whose core claim includes multi-agent isolation, that is a hole in the public surface, not a typo.

      Fix: patch `ScopedLunaris`'s `dsl()` in both SDKs to return the working builder pre-bound to the scope, and
      give the hand-written builders a scope they can carry. Needs the maturin+pytest / napi+vitest loops — neither
      crate is in `cargo test --workspace`.
- [ ] **W4.13** SDK bi-temporal time-travel has no passing test anywhere — **M** — NEW 2026-08-21
      **UPDATE 2026-08-22 — half answered, and the answer was two product defects, not a
      missing test.** The `as_of` half is what this entry describes and still stands. But the
      `.between()` half had a different cause entirely: F20 (the TS SDK could not construct an
      `Hlc` at all — napi drops integer-ness above u32::MAX, so every millisecond timestamp was
      rejected) and F21 (`TimelineReconstruction` cannot store a historical valid-time, so a
      historical window matches nothing). **F20 is FIXED** (`6db7b97`); the SDK-level bi-temporal
      CALL surface now works from TypeScript and is guarded end-to-end by
      `crates/lunaris-ts/__test__/hlc_bitemporal.spec.mts`. W4.13 stays open on F21 plus the
      refusal-test this entry asks for.
      `code_repo_memory_as_of_commit_50`'s golden pins `as_of = 2025-02-19`. `TemporalQuery::as_of` sets
      *system*-time as_of → `hydrate.rs` → `StoragePort::read_as_of` → `reject_historical_read` → `NotSupported`
      (`HISTORICAL_KV_READS = false`). Moon is the only backend, so the scenario cannot pass anywhere. It is
      skipped against the **named** gap rather than shipped as a known-red gate, and both driver files carry a
      `MOON_HISTORICAL_KV_READS` mirror constant for a one-line unskip when Moon gains the capability.
      Direct evidence for **W2.10**: it does not overturn the keyword decision (the prose at `README.md:257` already
      says as-of reads are search-side and graph-side while historical KV reads refuse), but it does mean the
      SDK-level time-travel story is documented and untested. Worth a test that asserts the *refusal*, since that
      is the actual contract.
- [x] **W4.14** `conversational_parity.spec.mts` still gates on the dead `LUNARIS_TEST_POSTGRES_URL` — **M** — NEW 2026-08-21
      The agent made the honesty-only fix (bail-outs → `ctx.skip`, zero assertion changes, cannot turn CI red) and
      deliberately stopped there. The Moon-only rewrite would un-skip 6 scenarios whose behaviour against 0.7.0 is
      genuinely unknown — the unsatisfiable-red trap ([[red-satisfiability]]). Do it as its own task with room to
      find out what those 6 actually do.
      **DONE 2026-08-22 (`2d0b8b8`).** Both conversational suites (TS + Py) rewritten Moon-only. The filed defect
      was one of FOUR, and fixing any three would have left the suites still not running:
      1. **Two spellings for one variable.** `LUNARIS_MOON_URL` vs `LUNARIS_TEST_MOON_URL` (and `MOON_URL` in the
         conformance layer — three names). CI exports the first; seven binding suites read the second, so even a job
         that named those files would have watched them skip. Unified on `LUNARIS_MOON_URL`.
         Collateral: `crates/lunaris/tests/moon_parity.rs` read the dead spelling, so its five tests — including the
         fresh-scope `unknown index` pin added in `ca801c1` three commits earlier — printed "skipping" in every
         workflow. `integration.yml` now runs `-p lunaris-memory --test moon_parity` in the job that has a Moon
         (note the package is `lunaris-memory`; the directory is `crates/lunaris`. My first draft of that step said
         `-p lunaris` and would have failed in CI — caught by running it locally, 5/5 green against a Moon on :6399).
      2. **The live-Moon job named ONE file per SDK.** `conformance-bindings.yml`'s `per-driver-parity` is the only
         job in the repo that starts a Moon and it ran `test_backend_parity.py` + `backend_parity.spec.mts`. The
         other ten live-Moon binding suites ran only in the `bindings-it` job, whose own step name says
         "offline / skip path". **That included `documentary_parity`, which W2.9 had just rewritten from an
         unsatisfiable gate into a satisfiable one** — a correct rewrite that changed nothing, because a satisfiable
         gate no job satisfies is the same green as an unsatisfiable one. This is the generalisation worth keeping:
         *making a gate satisfiable and making it satisfied are two separate pieces of work, and the first one alone
         looks exactly like success.*
      3. **`conversational_parity.py` was never collected.** pytest collects `test_*.py`; the name matched neither
         glob, so it had not executed since it landed — not skipped, absent. W4.15 correctly left the rename to this
         task, since renaming alone would have converted a silent no-op into a loud failure.
      4. The filed defect itself.
      Guard: `scripts/tests/test_live_moon_binding_tests_have_a_runner.py`, the sibling of
      `test_gated_tests_have_a_runner.py` one layer out. Asserts BOTH halves (the job exports the canonical variable
      AND runs every suite that reads it), plus a vacuity floor of 8 suites and a pytest-collectability check.

      **GREEN 2026-08-22, run `32526775483`:** Rust 1 passed · Python **73 passed, 3 skipped, 1 xfailed** ·
      TypeScript **68 passed, 1 expected fail, 3 skipped** (10/10 files). All three remaining skips are named
      product/prerequisite gaps (`HISTORICAL_KV_READS=false`; a bad-dim config; the granite GGUF the
      exit-cleanliness test exists to exercise). It took FOUR dispatched runs to get there, and each red board
      bought something the green one could not have:

      - run `32519504011` → napi camelCases method names and generates `static new(...)`; PyO3 does the same via
        `#[staticmethod] fn new`. Both SDKs' suites were written against a surface that does not exist.
        `generated.d.ts` actively misleads here (ledger F17).
      - → `backend_parity` asserted an ABSOLUTE count over the shared `_dev_` scope (`10` vs `163`). It held only
        because it was the only writer. Fixed in all three drivers; only TS had broken, by collection-order accident.
      - → `LUNARIS_EMBEDDER_OPENAI_URL` is read only under `embed-remote`, which no CI job compiled, so the two
        stub-embedder spy suites could never have passed.
      - run `32522481370` → no embedder means zero vectors means arbitrary top-k, and `source_prefix` is a
        POST-filter (PR #85). Three conversational suites passed and two failed on one run for the same reason: a
        coin flip. Fixed job-wide by `scripts/ci/stub_embedder.py`.
      - run `32524873697` → the last two failures were not test bugs at all. **F21** (`TimelineReconstruction`
        cannot ingest a historical valid-time) and **F22** (a zero-vector row outranks every real match, forever,
        and the Tier-0 default produces them). Plus **F20** on the TS side — the SDK cannot construct an `Hlc`.
        Together F20+F21 are the real content of W4.13.

      The three product defects are held by markers that FAIL when the defect is fixed — `xfail(strict=True)` in
      Python, `test.fails` in TS — with the original assertions untouched, so the fix and the restored assertions
      land in one commit. None of them is a skip.
      RED on 3 of its 4 tests naming all the above; GREEN after.
- [x] **W4.16** A failed capture is undiscoverable — the headline feature fails silently — **M** — NEW 2026-08-21
      Found the hard way: Moon 6381 refused **every** write for ~50 minutes on 2026-08-21 (`MOONERR diskfull`,
      the volume crossed the stall threshold) and nothing anywhere said so. Traced the whole chain:
      1. Moon rejects the write.
      2. `contextd` logs via `tracing` -> `init_tracing` in `crates/lunaris-hook/src/contextd.rs:188` pins
         `.with_writer(std::io::stderr)`, and the running daemon (pid 3449) has fd 1 **and** fd 2 on
         `/dev/null`. Verified with `lsof`. The warning is written into the void.
      3. `scripts/lunaris-codex-hook-adapter.py` calls `contextd_request(...)` for `capture_tool_*` and
         **discards the return value**, then `return 0` unconditionally. `contextd_request` itself returns
         `{}` on every `OSError`/`TimeoutError` and only writes stderr when `LUNARIS_CODEX_HOOK_DEBUG=1`.
      4. There is no hook-side log file at all — `~/.lunaris/logs/` contains only `moon-6381*.log`.
      Net: a capture that silently vanished is byte-for-byte indistinguishable from one that succeeded, which
      is [[built-not-wired]]'s sibling and the same class as the parked-test and bail-out-as-PASS findings.
      **Do not "fix" this by making the hook loud or non-zero** — a hook that breaks the user's session on a
      storage hiccup is worse than the bug. The failure needs to become *discoverable*, not fatal. Candidates:
      a rate-limited hook-side error log; a capture-failure counter surfaced in the SessionStart digest
      ("N captures dropped since <time>"); giving `contextd` a real log sink instead of `/dev/null`.
      The discriminating test is the one that matters: point a capture at a store that refuses writes and
      assert the operator can find out **from an artifact that outlives the process**.
      **DONE 2026-08-21.** Both causes addressed, because either alone leaves the bug: the failure moved from
      `debug!` to `warn!` (contextd's default filter is `warn`, so the level made it invisible even with a
      working stderr), AND it now appends to `~/.lunaris/logs/capture-failures.log` — an artifact that outlives
      the process and its `/dev/null` fds. Override `LUNARIS_CAPTURE_FAILURE_LOG`. Bounded: rotates to a single
      `.1` sibling past 64 KiB, so a store refusing every write cannot fill the disk it is already failing on.
      The hook stays quiet and zero-exit; nothing about the user's session changes.
      Path is a `ContextService` field, not an env read at the call site, so the test points it at a tempdir
      without `set_var` — the same process-wide hazard that raced `a_maintenance_compact` red today.
      Discriminating test exactly as specified: `context::tests::a_refused_capture_leaves_a_record_on_disk`
      drives a real capture through a `WriteRefusingStorage` answering every write with `MOONERR diskfull`, then
      asserts the record is readable from disk and names the error. Deliberately NOT "a warning was logged" —
      logs going nowhere was the whole defect. Mutation-verified: delete the record call and it fails with
      "left NO artifact at ...".
      Follow-up left open: the codex hook adapter still discards `contextd_request`'s return value, so a
      capture dropped on the ADAPTER side (socket refused, timeout) is still invisible. This closes the
      daemon-side half.
- [x] **W4.15** Three dead-test cleanups found in passing — **S** — NEW 2026-08-21 — DONE 2026-08-21.
      (c) turned out not to be cosmetic: `vitest.config.mts` set `poolOptions.threads.singleThread` for the
      stated purpose of serialising suites that mutate `process.env`, and `package.json` pins vitest **^4.1.8**
      — Vitest 4 REMOVED that option. The guarantee was not in effect and the race it prevents was live.
      Replaced with `fileParallelism: false`. (a) is documented rather than renamed: collecting the file would
      only turn a silent no-op into a loud failure, since every scenario drives the deleted Postgres backend —
      the rename belongs to W4.14's Moon-only rewrite.
      (a) `crates/lunaris-py/tests/conversational_parity.py` is **never collected** — the filename does not match
      pytest's `test_*.py`, so the whole file has never run. (b) `conformance-bindings.yml`'s header still
      advertises "THREE independent per-driver parity tests (Rust / Python / TypeScript)"; the Rust one
      (`crates/lunaris-recipes/tests/documentary_parity.rs`) was deleted in `03bf8bc` and only fixtures survive —
      it is two. Both SDK headers now say so; the workflow header still lies. (c) `crates/lunaris-ts/vitest.config.mts`
      uses `test.poolOptions`, removed in Vitest 4 — a DEPRECATED warning on every run.

## Standing constraints for every agent on this plan

- **Never touch ports 6379 / 6380 / 6381.** 6381 is the owner's live personal
  memory store (launchd `dev.lunaris.moon-6381`, Moon 0.8.5, ~1.3M keys). 6380
  is an ai-proxy Redis, not a Moon. Bench Moons live on 6399+ only.
- **Never `FLUSHALL` a Moon the task did not start.**
- **One llama.cpp inference process machine-wide** — concurrent Metal contexts
  deadlock on this box. Do not run inference tests while another agent might be.
- **The INTERNAL volume is the tight one — this was backwards until
  2026-08-21.** Measured that day: `/` (`/System/Volumes/Data`, where
  `/Users/tindang` lives) is at **98.8%, 6.0 GB free**, while `/Volumes/Games`
  has **456 GiB free**. The earlier version of this constraint claimed the
  reverse and sent every agent's `CARGO_TARGET_DIR` to the home volume;
  `lunaris-target-ga4` alone reached **23 GB** there (22 GB of it `debug/`),
  which is how the internal volume filled. Moon 6381's live store is another
  15 GB on that same volume, so filling it risks the owner's memory store.
  Still set `CARGO_TARGET_DIR=/Users/tindang/.cache/lunaris-target-<agent>` —
  a build tree on `/Volumes/Games` dies the moment that volume blips (see
  below) — but **`cargo clean` your target dir when your task ends**, and check
  `df -h /Users/tindang` before starting a large build.
- **`/Volumes/Games` is an external USB SSD that self-unmounts.** It dropped
  mid-bench on 2026-08-21, taking the worktree and a running gate with it. Any
  long job whose artifacts live there must tolerate the filesystem vanishing:
  `anygold_gate.sh` does (two-consecutive-ERR abort + free resume), ad-hoc
  scripts generally do not.
- **`MOON_TEST_BINARY=/Users/tindang/.lunaris/bin/moon`** — the test harness
  spawns its own ephemeral instance on a random port; it does not touch 6381.
  Inside a linked worktree the submodule build fails, so this is mandatory.
- Do not modify `vendor/moon`, `.add/`, or `.planning/`.
- Do not modify the owner's checkouts: `/Volumes/Games/tindang-repo/lunaris`
  (on `feat/lme-graph-fact-context`) or the helios-mono main working tree.
- `cargo test --workspace --exclude lunaris-py --exclude lunaris-ts`.
- Never `cargo fmt --all` — per-crate only. Clippy via `--workspace --all-targets`.
- Secrets (`NPM_TOKEN`, `MINIMAX_API_KEY`) are owner-handled: flag, never
  handle, never log.
- Red/green TDD. A red test must be **satisfiable** — walk the future fix
  through the harness before accepting the red.
- **Fresh worktrees need** `git -c protocol.file.allow=always submodule update
  --init vendor/moon` — plain `git submodule update --init` fails with
  `transport 'file' not allowed`.
- **INGEST-04's real form** is `grep -v '^\s*//' … | grep -c
  'storage\.atomic_write'` = 1. A bare `grep -c 'atomic_write'` returns 11
  because it counts doc comments; that is not the invariant.
- Commits: `git commit -F tmp/<slug>.txt`, footer
  `author: Tin Dang <tin.dang@trustifytechnology.com>`.

## ADD review sweep — findings (2026-08-21)

Reviewing PR #150 for case coverage rather than for correctness of the diff.
Both findings are the same defect class the branch keeps re-encountering: **a
check that reports nothing is indistinguishable from a check that passes.**

### S6 — conformance runners could skip themselves green (FIXED)

Every live-backend runner short-circuits to `Ok(())` when its backend is
unreachable. Correct on a laptop; wrong in `integration.yml`, where the Moon is
up and port-checked and the server is built before any test runs. There a skip
means the job stopped testing and reported success.

The stakes are concrete: the `/v1/forget` P0 is guarded by exactly one
assertion, `two_step_hard_delete`, which runs only inside
`run_protocol_lunaris_server.rs` — a file with **three** skip-to-green paths.
Six decision points total (including `src/bindings/mod.rs`, shared with the Py
and TS drivers) now route through `skip::skip_or_fail`; `integration.yml` sets
`LUNARIS_CONFORMANCE_STRICT=1`; `tests/no_silent_skip.rs` holds both halves.

Verified: strict proven both directions; guard mutation-verified on the SECOND
roster entry; full protocol suite runs 4.11s of real work under strict against a
live Moon.

### S7 — `probe_backend` tried only the first resolved address (FIXED)

`to_socket_addrs().next()`. On macOS `localhost` resolves `::1` first, so an
IPv4-bound Moon read as unreachable and the whole suite skipped green. All four
copies now try every address. Strict mode is what surfaced it — the skip had
been silent. Discriminated: `moon://localhost:6392` skipped before, runs after.

### S8 — `a_maintenance_compact` env race (FIXED, was the red board)

The one failing check on PR #150. `§2` set `LUNARIS_MOON_COMPACT_MIN` process-
wide while `§1` read it indirectly through the function under test. Gate now
passed as an argument. Mechanism proven deterministically, not by inspection.

### S9 — no post-forget absence assertion (FIXED)

`two_step_hard_delete` asserted the receipt (`rows_deleted == 1`) against a
genuinely seeded episode, which DOES discriminate the P0 — under the bug the
scoped scan matches nothing and reports 0. But every assertion read the
handler's own report of what it did, so a handler that reports a delete it never
performed passed. Step 4 now re-targets the deleted id and requires 404, the
contract `unknown_id_returns_404` already pins. Mutation-verified against a live
row.

### S10 — 12 of 15 python guards ran in no workflow (FIXED)

`scripts/tests/` holds 15 stdlib guards; workflows named three. The other
twelve — 66 passing assertions covering the hook adapters, capture signal gate,
contextd lifecycle and scope forwarding, turnkey verification, the Moon identity
probe and the session digest — executed nowhere. Not failing quietly; not
running. All 15 pass under `env -i` with an empty HOME, so wiring cost nothing.

ci.yml now discovers them by glob rather than listing three by name (a
hardcoded list is how the gap opened), and `test_gated_tests_have_a_runner.py`
— which existed to catch exactly this shape and did not police its own
directory — now asserts every guard has a runner, with a vacuity floor.

Two first-draft errors worth keeping: the RED passed because a *comment* in
ts-prebuild.yml ("No workflow ran scripts/tests/*.py before 0.6.1") satisfied a
presence check; and the first mutation was invalid because `unittest.main()`
exits before an appended `raise` runs, which nearly read as "the loop cannot
detect failures".

### S11 — nothing checked that npm entry points get published (FIXED)

`files` is an allowlist; anything absent is silently omitted from the tarball.
A package can declare `"require": "./lunaris.cjs"`, pass every test, publish,
and then throw MODULE_NOT_FOUND on first `require()`. Nothing packs a tarball
and inspects it, so `files` was the only thing standing in the way — and the
v0.6 rewrite had just added two new entry filenames by hand. Now ratcheted in
`test_npm_version_parity.py`, mutation-verified both ways (drop a file from
`files`; point an export at a nonexistent path).

### Open follow-ups from this sweep (not fixed here)

- **Four `probe_backend` copies.** Their comments call each other "verbatim
  mirrors"; they are not — `crash_recovery.rs` rejects `redis://` and unit-tests
  that, the others accept it. Consolidating would change behavior, so the
  IPv6 fix was applied four times instead. Worth unifying deliberately.
- **`run_storage_moon` assumes a pristine store.** Fixed keys plus an
  "expected None before any write" assertion, so a second run against the same
  Moon fails on `conformance:read_as_of:k1`. CI gets a fresh Moon per job, so it
  is invisible there and bites only locally.
- **CodeQL `permissions:` blocks** still missing on 5 workflows (ci,
  conformance-bindings, integration, perf-gates, recall-ratchet). Verified none
  needs write.
